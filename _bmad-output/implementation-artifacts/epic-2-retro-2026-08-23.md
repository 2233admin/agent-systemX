---
epic: 2
date: 2026-08-23
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 2 Retrospective — 控制面发布与自更新

> 本次运行以委托任务方式执行（无实时交互用户在场确认），因此按 headless 惯例处理：所有本应由人类确认的选择（Epic 选定、验收判定、拟议行动项）都记录在下方 Assumptions 一节，而非默默采纳。

## Epic summary

- **Epic**：Epic 2「控制面发布与自更新」（`_bmad-output/planning-artifacts/epics.md`；`_bmad-output/implementation-artifacts/epic-2-context.md`）。
- **Stories**：
  - Story 2.1「打包与发布流水线」— PR #133（`Eridanus117/configs-release-packaging`，已合并，commit `7e7fb1a`）。
  - Story 2.2「自更新客户端」— PR #135（`Eridanus117/sprint-status-2-1-done`，标题为 Story 2.2，分支名系历史复用，已合并，commit `89e88ea`）。
- **`detect-epic --epic 2`**：`story_count: 2`，`done_stories` 含 `2-1-打包与发布流水线`、`2-2-自更新客户端`，`pending_stories: []`（完整性门通过），`retro_key: epic-2-retrospective`，`retro_status: optional`。
- **归属提交（人工核实，非单一线性 range）**：Story 主体提交为 `7e7fb1a`（Story 2.1）、`89e88ea`（Story 2.2）；另有三个直接归属本 Epic 的追溯修复/修正提交：
  - `ccae699`（无 PR，直接推送到已合并分支后的修复）—— 修复 `gh release create` 误用不存在的 `--clobber` 参数，导致首次真实发布尝试（`configs-v0.1.0`）在参数解析阶段失败退出。
  - `327844c`（PR #144）—— 修复编译后独立可执行文件因 `import.meta.url`+`readFileSync` 在 Bun 虚拟文件系统下路径失效而无法打开 SQLite（自 Story 2.1 首次发布起即存在），并加固 `release-configs.yml` 冒烟测试新增 `configs list` 真实执行步骤。
  - `fd6f7fab`（PR #145，correct-course）+ `a8560f1`（PR #148）—— 将"成功路径也不得输出"这条 Story 2.2 spec 自行加码的越界约束修正为"失败静默、成功打印一行提示"，并落地对应代码/i18n/测试。
  - 尝试过用 `git_evidence.py --range 8e157a9..46049d3` 取一条完整线性 range，但该 range 混入了大量与 Epic 2 无关的交错提交（Epic 3 Story 3.1、BMad 工具链 chore 提交等，`commit_count: 33`），**记录该窄化**：本次 Findings/Behavior check 一律只针对上述 5 个明确归属提交及其触达的文件集合，不采用未过滤的线性 range 统计。
- **证据清单**：
  - Epic spec / Story spec：可读。`epic-2-context.md`、`spec-2-1-打包与发布流水线.md`、`spec-2-2-自更新客户端.md`（均含 Spec Change Log）、`sprint-change-proposal-2026-08-23-configs-self-update-visible-success.md`。
  - Sprint status：可读，`epic-2: in-progress`（与两个 Story 均 `done` 不一致，同 Epic 1 先例，属已知工具缺口，见下方 Previous-retro follow-through）。
  - 先前 retrospective：`epic-1-retro-2026-08-22.md` 存在，已核对其 action items 现状（见下）。
  - Session logs：**不可用**。5 个归属提交全部单提交直推/单提交 PR，`gh pr view {133,135,144,145,148} --json comments,reviews` 全部返回空数组，无 review 讨论串。与 Epic 1 同款盲区，按此收窄。
  - **生产证据（Epic 1 没有的额外证据源）**：仓库已存在 9 个真实 GitHub Release（`configs-v0.1.0` ~ `configs-v0.1.8`，`gh release list` 核实），每个均带四个平台资产 + `SHA256SUMS.txt`（以 `configs-v0.1.8` 核实资产列表）。这意味着打包/发布/校验链路不只是单元测试通过，而是被真实使用路径反复验证过——`ccae699`/`327844c` 两个修复正是从真实发布尝试中暴露出来的。

## Findings

### Aggregate views

**Architecture delta** — 新增 `packages/control-plane/src/adapters/self-update/`（4 个纯函数/类文件：`asset-target.ts`、`checksum.ts`、`version-compare.ts`、`github-release-updater.ts`）与 `SelfUpdatePort`（`application/ports.ts`），遵循既有六边形约定（adapter 实现端口、不做产品决定、`checkAndApply` 不抛出）。CLI 接入点集中在 `cli/index.ts` 的 `import.meta.main` 块与新导出的 `reportSelfUpdateResult`，未新增常驻进程/daemon/轮询，符合 epic-2-context.md "本 Epic 不引入 daemon" 的约束。未发现违反六边形边界的跨层依赖。

**Duplication map** — 未发现新增重复代码：`asset-target.ts`/`checksum.ts`/`version-compare.ts`/`github-release-updater.ts` 各自单一职责，逐一读取源码确认无逻辑重叠。`327844c` 修复的 SQL 导入方式在 `repository.ts`/`launch-repository.ts` 两处并行存在（各自独立的 migrations 导入改造），但这是延续既有 Story 1.1/1.2 就已重复的 `Fact<T>` 序列化模式（Epic 1 retro action item 4 已记录该重复，未扩大），本 Epic 未引入新的重复面。

**God-class / size growth** — 按提交级 `numstat` 核实：`github-release-updater.ts` 226+16 行、`version-compare.ts` 52 行、`checksum.ts` 36 行、`asset-target.ts` 23 行、`cli/index.ts` 本 Epic 净增约 66 行（`9+11+46`，跨三个提交），均为小文件、单一职责，未见 god-class 迹象。`.github/workflows/release-configs.yml` 从 113 行增至 126 行（`327844c` 加固冒烟测试 +13 行），仍是单一 job、结构清晰。

**Pattern divergence** — 与既有 `FakeOmpProcessPort` 风格一致的构造函数注入（`fetchFn`/`execPath`/`platform`/`arch` 均可覆盖，生产默认值不可变），`checkAndApply` 顶层 try/catch 静默降级的写法与其他 adapter 的"不做产品决定、不抛出"约定一致。未发现偏离仓库既有 TypeScript/Bun 约定的新模式。

**Spec-to-implementation reconciliation** —
- 抽查 spec-2-1 三条声明式验收标准：`bun src/cli/index.ts --version` 本次直接重新执行核实打印 `dev`、退出 0；发布 tag 场景不再靠"人工核对语法"验证，而是被 9 次真实 `configs-v*` 发布直接证实（含两次真实失败-修复循环）——**证据强度超过 spec 原定的验收方式**。
- 抽查 spec-2-2 七条 Given/When/Then：`tests/adapters/self-update.test.ts` 覆盖全部 I/O 矩阵行 + 两条系统级场景（旧版本 latest release、写入失败无中间态），本次重新运行全部通过（见 Behavior check）。`isNewerVersion` fail-closed 语义与 Spec Change Log 描述的"CI 自烧毁场景"修复一致，代码与文档一致。
- **发现一处文档漂移（新，fix-now/流程）**：`epic-2-context.md` 第 12 行仍写"Story 2.2（done，**正在按 2026-08-23 correct-course 修正**"成功路径需打印一行提示"这一实现细节）"、第 34 行仍称该修正"是本 Epic **当前唯一待落地**的行为修正点"——但该修正已经完整落地并合并（`fd6f7fab`/PR #145 修 spec，`a8560f1`/PR #148 落代码，均已确认合入 `46049d3`）。`epic-2-context.md` 描述的"进行中"状态已过期，与 Epic 1 retro 记录过的"编译产物 context 落后于实际交付状态"是同一类漂移模式的重演。**Fix-now**，见 Action Items。
- **发现一处 spec 文档内部引用漂移（新，低严重度）**：`spec-2-1-打包与发布流水线.md` 的"Suggested Review Order"引用 `release-configs.yml:84` 处存在 `--clobber` 参数，但 `ccae699` 已经把该参数删除（`gh release create` 从不支持 `--clobber`，只有 `gh release upload` 支持），当前 workflow 文件里已不存在这一参数；该 spec 文件本身未随 `ccae699` 修订同步更新引用行号/内容。已核实为纯文档引用漂移（代码本身正确、已发布 9 次证明），不影响任何已声明验收标准。**Accept-as-is / 低优先级文档清理**，不单独开 action item（成本低于收益，随下次接触该 spec 时顺手改更合适）。

### Diff-scope review

未调用 `bmad-review` 子技能（本次以内联方式对 5 个归属提交的完整 diff 做窄范围审查，记录该窄化）。已完整读取 `github-release-updater.ts`、`asset-target.ts`、`checksum.ts`、`version-compare.ts`、`cli/index.ts` 的 `reportSelfUpdateResult`/自更新接入块，以及 `release-configs.yml` 全文：

1. **[已确认为真实历史缺陷，均已修复，不构成待办] 两处只有真实发布才能暴露的缺陷**：
   - `ccae699` 修复的 `gh release create --clobber`（该参数只属于 `gh release upload`，`gh release create` 从未支持）——首次真实 tag 推送（`configs-v0.1.0`）直接在参数解析阶段失败，`bun test`/`bunx tsc --noEmit`/`actionlint`（若可用）均不会捕获这类"CLI 参数存在性"错误。
   - `327844c` 修复的 `import.meta.url`+`readFileSync` 在 `bun build --compile` 虚拟文件系统下失效（迁移 SQL 文件未被打进产物），该 bug **从 Story 2.1 第一次发布起就存在**，`bun test`/`tsc` 同样测不出（测试永远跑源码，不跑编译产物）。
   - **信号**：两处缺陷的共同根因是"Story 2.1 声明的验收标准（本地未编译运行 + 显式子命令不受影响 + workflow 语法人工核对）从未包含'实际编译并运行产物、实际推送一次 tag'这一类验证"，而这类验证只能通过真实发布才能触发。`327844c` 已经为 SQLite 场景加了编译产物冒烟测试，但没有等价的"`gh` CLI 参数正确性"防呆。**这是一条新的、值得记录的流程教训**，见 Action Items。
2. **[已核实，无新发现，deferred-work.md 已完整记录，不重复开待办]** 逐一核对 `deferred-work.md` 中 Story 2.1/2.2 相关的既有条目（`--version` 无短别名、`CONFIGS_VERSION` 无防呆机制、发布产物未上传 workflow artifact、`GET /releases/latest` 未按 `configs-v` 前缀过滤、无退出开关/节流/verbose 日志、`.bak` 文件永不清理、两次 rename 间的极小非原子窗口、无并发锁）——本次重新审阅源码，确认这些条目描述与当前代码状态**仍然一致**，均已有明确 disposition（accept-as-is 或 defer，需要负责人裁决），不构成新发现，不重复记录。
3. **[accept-as-is，已核实]** `replaceBinary` 采用"先写临时文件、再两次 rename"顺序，与 Windows"运行中可执行文件不能被直接覆盖，需先重命名旧文件"的约束一致（`epic-2-context.md` Requirements 明确写出该约束）；`mode: 0o755` 在 Windows 上是 no-op，跨平台无害。

### Behavior check

- `bun install`：本次执行环境缺少依赖（`react/jsx-dev-runtime` 模块缺失导致 `tui.tsx` 相关测试报错），已执行 `bun install` 补齐（126 个包），非代码缺陷，是执行环境问题，修复后不再复现。
- `bunx tsc --noEmit`（`packages/control-plane`）：干净退出，0 错误，独立重新执行确认。
- `bun test`（全量）：**272 pass / 0 fail / 920 expect() calls，23 个测试文件** —— 独立重新执行确认，含本 Epic 三个测试文件（`tests/adapters/self-update.test.ts`、`tests/cli/self-update-notice.test.ts`、`tests/cli/version.test.ts`，合计 24 项测试全部通过）。
- 直接运行 `bun src/cli/index.ts --version`：打印 `dev`，与 `src/cli/version.ts` 提交态常量一致，确认自更新检查在源码/测试运行时被跳过（`checkAndApply` 的 `currentVersion === 'dev'` 早退分支，配合本次源码通读确认逻辑正确）。
- **真实生产行为证据**：`gh release list` 确认 9 次真实发布（`configs-v0.1.0`~`configs-v0.1.8`），`configs-v0.1.8` 资产列表核实四平台二进制 + `SHA256SUMS.txt` 齐全。两次真实缺陷（`--clobber` 参数错误、SQLite 迁移路径失效）均由真实使用触发并修复，这本身就是自更新/打包链路端到端被真实验证过的强证据，比"手动 checks (if no CLI)"里描述的"交叉编译一个虚构旧版本号的 smoke 二进制"更充分。未执行一次真正触发 `checkAndApply` 完整替换路径的端到端测试（会修改本机真实二进制），按 spec 的 Ask First 约束不在本次 retrospective 范围内主动执行。

## Previous-retro follow-through

对照 `epic-1-retro-2026-08-22.md` 的 7 条 action items 与当前 `sprint-status.yaml.action_items`：

| id | 当前状态 | 本次核实 |
|---|---|---|
| epic-1-retro-item-1（`launchOmp` 缺类型化 catch） | `done` | 与 Epic 2 无关，未重新核验，采信既有记录 |
| epic-1-retro-item-2（`parseCapabilityJson` 校验） | `done` | 同上 |
| epic-1-retro-item-3（`readYesNo` stdin EOF） | `done` | 同上 |
| epic-1-retro-item-4（`factColumns` 重复） | `open` | 与 Epic 2 无关，仍未处理，不在本次范围内处理 |
| epic-1-retro-item-5（`sprint_status.py` 缺 `--set-epic-status`） | `open` | **本次直接复现**：`epic-2`/`epic-3` 均已两个 Story 全 `done` 但 `development_status.epic-2`/`epic-3` 仍是 `in-progress`，与 item-5 描述的工具缺口完全一致，证实该缺口仍未修复且持续影响后续 Epic |
| epic-1-retro-item-6（过程教训，未经证实） | `open` | 与 Epic 2 无关 |
| epic-1-retro-item-7（CAP 双重职责前瞻风险） | `done`，`resolved_ref` 指向 Epic 4 的 correct-course 提案 | 已核实 `resolved_ref` 文件存在（`sprint-change-proposal-2026-08-23-cap-claude-codex-adapter.md`），确认该项已被后续规划实际吸收，不是仅打勾未落地 |

本次为 headless 运行，**不**传递 `--set-action-status`（无新证据支持任何一项从当前状态转移，item-5 只是被重新证实仍然存在，不是"已解决"）；上表记录供负责人参考。

## Action items

1. **[fix-now，文档，新发现] `epic-2-context.md` 对"成功路径可见提示"这条修正的描述已过期** — 第 12/34 行仍称该修正"正在进行"/"当前唯一待落地"，但 `fd6f7fab`（PR #145）+ `a8560f1`（PR #148）已完整落地并合并。建议下次运行 `compile-epic-context`（或手动编辑）刷新这两处措辞为"已完成"。**owner**：repo owner / 下一次 compile-epic-context 运行；**disposition**：fix-now（低成本文档修正，不阻断任何交付）。
2. **[流程教训，新发现] Story 2.1 的两个真实缺陷（`gh release create --clobber` 参数错误、编译产物 SQLite 迁移路径失效）都只能靠真实发布尝试才能暴露，Story 自身声明的验收标准（本地源码运行 + 显式子命令回归 + workflow 语法人工核对）对这两类"CI 环境/编译产物特有"的缺陷类别没有覆盖。** `327844c` 已经为 SQLite 场景加了编译产物冒烟测试（`configs list` 真实执行），但没有等价的"`gh` CLI 参数正确性"防呆（如本地跑一次 `gh release create --help` 核对参数存在性，或在 PR 描述里显式要求"发布类 workflow 改动至少要有一次真实 tag 推送验证"）。**owner**：repo owner / 下一次 CI 工具迭代；**disposition**：defer（成本收益上是可选加固；当前风险已通过"低版本号 tag 逐步验证再正式发布"这一实际操作习惯部分缓解，9 次真实发布已经把该类缺陷筛出来了）。
3. **[carry-forward，非本 Epic 新增] `sprint_status.py` 缺少 `--set-epic-status`（epic-1-retro-item-5）在 Epic 2/3 上再次复现** — 不作为独立新 action item 重复记录（避免与 item-5 语义重复），仅在上方 Previous-retro follow-through 表格中标注复现证据，供负责人判断是否提升该项优先级。

## Acceptance verdict

**accepted-with-open-items**

- **Criteria**：declared —— `spec-2-1-打包与发布流水线.md`（3 条声明式验收标准）与 `spec-2-2-自更新客户端.md`（7 条 Given/When/Then）均有明确验收标准；`epics.md` 中 Epic 2 正文本身只是占位段落（"具体 Story 与验收标准由后续细化产出"），验收标准的权威来源是两份 spec 文件。
- **完整性门**：通过 —— `pending_stories: []`，两个 Story 均 `done`。
- **证据支持**：`bunx tsc --noEmit` 与 `bun test`（272/272，含本 Epic 24 项）独立重新执行通过；spec-2-1 三条验收标准与 spec-2-2 七条验收标准逐一核对与代码/真实发布记录一致；correct-course 修正的"成功路径打印提示"已核实真正落地在代码（`github-release-updater.ts` 返回 `string | null`、`cli/index.ts` 的 `reportSelfUpdateResult`、`i18n.ts` 双语 key）而非仅停留在 spec 文本；9 次真实 GitHub Release 提供了比单元测试更强的端到端证据，且两次真实使用中暴露的缺陷（`--clobber` 参数、SQLite 迁移路径）均已修复并被后续冒烟测试部分覆盖。
- **未阻断的 open items**（均不违反任何已宣告的 Given/When/Then 验收条件，因此不构成 rejected 的理由）：
  1. `epic-2-context.md` 的过期描述（fix-now，纯文档）。
  2. "CI 环境/编译产物特有缺陷类别缺乏专门防呆"这一流程教训（defer，可选加固）。
  3. `deferred-work.md` 中已记录的自更新残余风险（跨进程竞态、`.bak` 累积、`releases/latest` 未按前缀过滤、无退出开关/节流/verbose 日志）——均已有明确 disposition，需要负责人裁决才能推进，本次未发现新增或恶化的实例。
  4. `sprint_status.py` 缺少 Epic 级状态转换机制（epic-1-retro-item-5 的复现，非本 Epic 独有）。

  这些均不构成"验收条件未满足"或"存在阻断性发现"，因此不构成 rejected 的理由；但也不是"零遗留项"的纯粹 accepted，故为 **accepted-with-open-items**（与 Epic 1 verdict 一致）。

## Open questions

- `epic-2-context.md` 与其他"编译产物"类文档（如未来 Epic 的 `epic-N-context.md`）是否应该在对应 correct-course/修正 PR 合并时同步刷新，而不是留到下次 retrospective 才被发现过期？这是 Epic 1（sprint-status 落后于实际 Story 状态）与 Epic 2（epic-2-context.md 落后于实际修正状态）连续两次出现的同一类"编译产物漂移"模式。
- 是否值得为"发布类 workflow 变更"建立一个比"人工核对语法"更强的验证要求（如强制要求至少一次真实/测试 tag 推送才能把 Story 标记为 `done`）？还是接受当前"先发低版本号 tag 探路"的实际操作习惯已经是足够的实践性缓解？

## Assumptions

（本次以委托任务方式运行，无实时交互用户在场确认，按 headless 惯例记录以下本应由人类确认的选择）

- **Epic 选定**：由调用方在 Skill 调用参数中明确指定 Epic 2，未走自动探测路径；已用 `detect-epic --epic 2` 核实该 Epic 存在且 `story_count: 2`。
- **`pending_stories` 结果**：`[]`（空），未触发"未完成 Epic"确认流程。
- **归属提交集合的窄化**：未使用 `git_evidence.py` 返回的完整线性 range（`8e157a9..46049d3`，`commit_count: 33`，混入 Epic 3/chore 提交），改为人工核实归属到 5 个提交（`7e7fb1a`/`89e88ea`/`ccae699`/`327844c`/`fd6f7fab`+`a8560f1`）；此窄化未经用户确认。
- **`bmad-review` 子技能未调用**：改为对上述 5 个提交的 diff 做内联审查，范围窄化未经用户确认。
- **机器验收判定**：`accepted-with-open-items`，未经人类在本次会话内确认——已在上方"Acceptance verdict"节给出完整证据链，供负责人事后复核/否决。
- **拟议 Action Items**：共 2 条新增（1 条 fix-now 文档、1 条流程教训），另有 1 条对 epic-1-retro-item-5 的复现记录（不作为独立新增项），均为 *proposed*，未被应用/执行，也未被任何人类在本次会话内确认为"应该做"。
- **未运行 Phase 3 团队讨论**：按 skill 定义默认跳过，本次委托任务未明确要求"as a team"讨论，因此跳过。
- **Previous-retro follow-through 的 `--set-action-status`**：未传递该参数——item-5 只是被重新证实仍然存在（复现），不构成"已解决"的确认证据，其余各项与本 Epic 无直接新证据，因此不建议任何状态转移。
