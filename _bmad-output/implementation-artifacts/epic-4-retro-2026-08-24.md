---
epic: 4
date: 2026-08-24
verdict: accepted-with-open-items
criteria: profiled
headless: false
---

# Epic 4 Retrospective — 装配并激活 Claude Code 客户端

## Epic summary

**Epic：** Epic 4 — 装配并激活 Claude Code 客户端（`_bmad-output/planning-artifacts/epics.md`）。

**Story 列表（全部 8 个，均 `done`）：** 4.1 建立 Claude Code adapter 骨架与硬控制能力探测；4.2 装配 Claude Code 的确定性 AdapterPlan；4.3 fresh target 的启动与观察；4.4 already-running session target 的 requires-restart 路径；4.5 `.cap/` parity 验证与本仓自身切换；4.5b Claude adapter 内容物化能力；4.6 `configs` CLI 的 Claude 入口；4.7 退役 `.cap/` 本体。`sprint_status.py detect-epic --epic 4` 确认 `story_count: 8`、`pending_stories: []`——完整性门通过。

**Diff range：** `8fae93496848af38ba3fb76cf68d41419e9a7132..478f5a3c7d99dd18dbb18cc66ccf4cda1bd2e83c`（Story 4.1 首个提交的父提交，到 Story 4.7 最后一个提交）。`git_evidence.py` 报告：27 个提交（5 个 merge，其中 4 个被测量——1 个 merge 未被测量，是 `merges_measured < merge_count` 的已知缺口，其 churn 未反映在 `files`/`merge_files` 汇总里）。范围内混有非 Epic 4 的提交（Windows TUI/PowerShell 输入修复、`configs` 自更新修复等，均来自并行会话，与 Epic 4 无关，`stories` 字段为空数组，未被误算进任何 Story）。

**证据清单：**
- Epic spec：`epics.md` Epic 4 小节——本身经历过一次 2026-08-24 correct-course 重大改写（原 4 个 Story 变 8 个，退役顺序四步收窄三步），当前版本即验收依据。
- Story 文件：`spec-4-1` 至 `spec-4-7`（含 `spec-4-5b`）共 8 份，全部 `status: done`，均带 Review Triage Log 与 Auto Run Result。
- 架构产出：`ARCHITECTURE-SPINE.md` 新增 AD-21（内容物化）、AD-20 澄清；`sprint-change-proposal-2026-08-24-cap-retirement-redesign.md`（correct-course 产出）。
- Sprint status：`4-1`～`4-7` 全部 `done`；本回顾已完成（`epic-4-retrospective: done`），因此按 sprint-status 顶部定义“所有列出 stories done”将 `epic-4` 收口为 `done`。回顾 verdict 仍为 `accepted-with-open-items`，未完成 action item 继续保留，不由父级状态掩盖。
- 前序回顾：`epic-1-retro-2026-08-22.md`、`epic-2-retro-2026-08-23.md` 均存在且其 action item 状态以 `sprint-status.yaml` 当前记录为准；Epic 3 尚无回顾（`epic-3-retrospective: optional`），不影响 Epic 3 已列 stories 全部完成的父级状态定义。
- Session logs：本 Epic 全程在**同一个连续会话**内完成（本轮对话本身），协调者对每个 Story 的实现子代理调度、review 触发、独立复核过程均有第一手记忆，不依赖外部日志文件；这份记忆是本次回顾 Phase 2 的重要输入，但严格标注为"协调者一手观察"而非可独立复现的文件级证据，与其他有具体 file:line 出处的发现区分对待。

**已知不寻常但已澄清的过程事实：** Story 4.5 最初的 AC2（"本仓自身切换"）在 review 阶段被发现对象不存在（本仓自身 session 的装配与 `.cap` 无渲染管线关联），触发了一次 correct-course（`sprint-change-proposal-2026-08-24-cap-retirement-redesign.md`）与一次架构补写（AD-21），把原本 6 个 Story 的 Epic 4 拆分/新增为 8 个。这不是本次回顾要"发现"的缺陷，而是过程本身产生的、已经被走完流程处理掉的真实修正。

## Findings

### God-class / size growth（协调者直接计算，来自 `git_evidence.py` 的 `files` 汇总 + 当前文件行数）

- **`packages/control-plane/src/application/claude-launch.ts`：627 行，本 Epic 内 3 个提交贡献 649 行新增（净 +627）。** 对照它明确设计要"结构同构"的 OMP 对应文件 `src/application/launch.ts`（424 行）——Claude 版本比 OMP 版本大约 48%，尽管两者意图上是同一形状的 fresh/already-running 启动编排逻辑。三次独立贡献分别来自 Story 4.3（fresh 路径骨架）、Story 4.4（already-running 分支）、Story 4.5b（内容物化接入），每次单看都是合理增量，没有一次单独触发"这个文件是不是太大了"的判断。**评级：候选但非确定缺陷**——较大部分原因是 Claude 侧确实比 OMP 多做一件事（AD-21 内容物化在 OMP 侧没有对应物，OMP 走原生 `--skills` 按名装配，不需要物化），文件更大有实质理由，不是纯粹的意外膨胀；但尚未被任何一次 Story 级 review 从"结构该不该拆"的角度审视过，值得记录以备后续如果继续膨胀时参考。
- **`packages/control-plane/src/cli/index.ts`：1169 行，本 Epic 内贡献约 233 行新增（212 直接提交 + 21 经由 merge 提交）。** 这个文件是全部 Story（Epic 1～4）共享的唯一 CLI 组合根，本 Epic 只是最新一批贡献者；`runClaudeLaunchFlow` 被直接加进这同一个文件，跟随 Epic 1 的 `runLaunchFlow`、Epic 3 的 `runEstablish`/`runRevise` 挤在一起，没有像 Claude 领域逻辑本身那样被拆進 `adapters/clients/claude/` 独立模块。**评级：跨 Epic 累积的既有趋势，非本 Epic 独有的新缺陷**——本 Epic 只是照既定惯例（`runLaunchFlow` 同样在这个文件里）添加了 `runClaudeLaunchFlow`，没有打破约定，但这个文件本身已经过了一般认为舒适的单文件规模，值得作为跨 Epic 的架构层面观察记录。

### 子代理停滞模式（协调者一手观察，非文件级证据——见 Epic summary 的证据清单说明）

本 Epic 全程由协调者亲自调度 8 个 Story 的实现子代理（部分含额外的独立 review 子代理），反复观察到同一个失败模式：Story 3.3、4.2、4.4 的实现子代理在完成实现后，误以为自己派生的 review 子代理会"自动"把结果传回来，于是以"我会等待通知"收尾——但这类子代理没有能唤醒自己的机制，实际上就是卡死，需要协调者手动 `SendMessage` 才能让它继续；Story 3.2、4.5b、4.6、4.7 还额外出现"孙代理"（子代理自己派生的下一层子代理）尝试用 `"general-purpose"` 这个 agent *类型名*（不是可寻址的会话名/id）回传结果，导致消息投递失败，孙代理的产出因此需要协调者手动读取、转发给子代理。这与 `epic-1-retro-item-6` 描述的问题是同一类，且在本 Epic 反复出现了至少 7 次（跨 5 个不同 Story）。详见上方 Previous-retro follow-through。

### Spec-to-implementation reconciliation（子代理独立核实，逐条给出 file:line 出处）

针对 5 项具体声明逐一核对 spec 文字与真实代码，结论：**全部 5 项确认一致，无一处静默偏离**——`resolveClientSupport('claude-code')` 真实返回 `{supported:true}` 无 `reason`；AD-21 物化合同（`materialized/plugin/.claude-plugin/plugin.json` + `skills/<name>/`、Instructions 直传文本、MCP 写 `mcp.json`、原子写入）逐条对应到 `content-materializer.ts` 真实实现；`.cap/` 确认不存在，13 个归档 spec 与 3 个保留 spec 精确匹配 `openspec/specs/`/`_archive/openspec/specs/` 当前目录内容；退役顺序文本确认已是三步，没有遗留"本仓自身切换"作为待办的措辞；Codex 边界确认守住（`adapters/clients/codex/` 不存在）。

另外扫描出一处**在首次回顾核验时真实存在、但从未被隐瞒**的缺口：当时 `materialized/` 目录确实没有清理代码，AD-21 的 Rule 原文承诺"调用达到任一终态后，`materialized/` 随其余 invocation 目录一并清理"，而 `src/adapters/system/claude-invocation-dir.ts` 尚无删除逻辑。该历史发现与首次 spec 残留风险记录均予保留；随后在本回顾第二轮 fix outcome 中已补齐 `ClaudeInvocationDirPort.cleanup`、`FsClaudeInvocationDirPort` 的递归删除实现，并由 `launchClaudeFresh` 的 `finally` 调用，当前代码不再符合“未清理”的旧状态。

### 命名重构/死分支等此前已披露决定 —— 复核确认执行到位，非新发现
子代理同时复核了 Story 4.2→4.3 的 `ClaudeAdapterPlan`→`ClaudeAssemblyManifest` 改名传播（确认无命名冲突残留）与 `determineClaudeLaunchTarget` 的 `'fresh'` 分支当前不可达（确认与 Story 4.4 自己披露的设计取舍一致）——两者均已在对应 Story 的 spec 里明确记录过，本次只是复核"记录的决定是否真的被执行"，结论是肯定的，不构成新发现。

### 架构/重复/风格三视角（子代理独立derive，逐条 file:line 出处；多数发现已被本 Epic 自己的注释提前披露并给出理由，只挑出真正未被解释、或值得跨 Story 记录的部分）

- **`application/claude-launch.ts` 绕开了架构图要求的 port 间接层，直接调用 `content-materializer.ts` 的具体实现。** 该文件里其余每一个副作用协作者（`ClaudeProcessPort`、`ClaudeCapabilityProbePort`、`ClaudeLaunchContextWriter`、`ClaudeInvocationDirPort`）都经由 `LaunchClaudeFreshDeps` 注入端口访问，唯独 `materializeClaudeContent`（真实执行 `readFile`/`writeFile`/`cp`/`mkdir`/`rename` 等文件系统 IO）是直接从 `adapters/clients/claude/content-materializer.ts` import 函数调用，没有对应端口接口——测试文件里能看出这个后果：真正做内容物化的用例只能退化成用真实 `mkdtemp` 临时目录，而不是像其它协作者那样注入 fake（`tests/application/claude-launch.test.ts:180-208`）。这是本 Epic 内**没有任何注释解释为什么**的一处真实架构不一致，其余类似的直接依赖（`adapter-plan.ts`/`assembly-manifest.ts`）好歹是"零 IO 纯函数"，理由站得住，这个是唯一一个有真实 IO 却跳过端口的。**建议列为 action item。**
- **`FsLaunchContextWriter`（OMP）与 `FsClaudeLaunchContextWriter`（Claude）几乎逐字重复**（`mkdirSync` + `JSON.stringify` + `writeFileSync`，仅目录名与类型参数不同），且是本 Epic 里少数几处重复中**唯一没有注释说明"为什么不共享"**的——本 Epic 其余重复（三处哈希函数、`runLaunchFlow`/`runClaudeLaunchFlow`、`deriveClaudeObservedOutcome`、`--strict-mcp-config` 双重贡献）都在代码注释里明确论证过"为什么故意不合并"，唯独这一对没有。**建议列为 action item**（提炼一个泛型 `FsJsonLaunchContextWriter<T>` 或至少补一句说明为什么不这么做）。
- **`content-materializer.ts` 的 `writeFileAtomic` 与 `adapters/self-update/github-release-updater.ts` 的 `replaceBinary` 重新实现了同一套"同目录临时文件 + rename"纪律**，前者注释里点名引用了后者作为设计依据，但代码本身仍是两份独立实现（各约 6 行）。比上一条轻微（至少有引用说明），但仍是真实可提炼的小重复，**建议记为 defer**（价值不高，不阻塞）。
- **`launchOmp` 从未校验 `plan.client === 'omp'`，是一个真实存在、本 Epic 自己的代码注释已经点名承认但明确不在本 Epic 范围内修的 OMP 侧缺口**（`claude-launch.ts:406-410` 的注释与 Story 4.6 spec 的 deferred 条目完全一致，子代理独立复核后确认属实）——这与 Story 4.6 review 阶段已经记入 sprint-status.yaml deferred 的那条是**同一个问题**，不是新发现，在此合并记录、不重复计数。
- **`adapters/omp/` 与 `adapters/clients/claude/` 目录路径不对称**（架构脊柱文本写的目标路径是 `adapters/clients/omp/`，但 OMP 实际代码在 `adapters/omp/`，从未迁移）——这是 OMP 侧遗留问题，不是本 Epic 引入的，本 Epic 的新代码反而是唯一严格遵循了脊柱文本路径约定的部分。**记录为跨 Epic 观察，不建议本 Epic 补救。**
- 其余发现（`ClaudeCapabilityProbeResult` 比 OMP 探测结果字段更丰富、测试文件命名因两级目录结构被迫加前缀、`FsClaudeLaunchContextWriter` 测试覆盖比 OMP 对应物更细致、`claude.hook-deny-return-value` 的 argv 归属规则被显式记录）均确认是有明确理由或结构性必然、非缺陷。

### 跨 Story 边界 review（`code-review` 高强度 fork，专门要求聚焦"单个 Story review 看不到的边界"；两条最严重发现均由协调者亲自逐行核实确认属实，非道听途说）

- **【已验证属实，最严重】一次真正成功的 Claude 启动，最终状态展示会自相矛盾。** `configs use/switch --client claude-code` 成功后，`runClaudeLaunchFlow` 复用了"客户端中立"的 `getLaunchStatus`（`application/launch.ts:366-394`）——但 `getLaunchStatus` 内部第 378 行无条件调用的是 **OMP 专属**的 `computeKnownDifferences(revision)`（同文件第 104 行），这个函数只要 `revision.instructions`/`mcp`/`hooks`/`plugins` 非空就恒定报告"未物化"，完全不知道 Story 4.5b 已经为 Claude 建了一套真正做物化判断的 `computeClaudeKnownDifferences`（`claude-launch.ts:103-128`）——但那个函数的结果只在 `launchClaudeFresh` 内部用于计算 `applyResult`，从未通过 `ClaudeLaunchOutcome` 往外传。协调者直接读代码确认：`getLaunchStatus` 的类型签名（`LaunchStatusDeps`/`Fact<string>`）看起来客户端中立，但函数体内部并不是。**真实后果**：一次 Skills/Instructions 真的物化成功、`launchClaudeFresh` 返回 `succeeded` 的启动，最终打印的状态会同时显示"Apply result: applied"和"Known differences: - instructions-content-not-materialized-in-mvp"这种自相矛盾的文字。`tests/integration/cli-claude-launch.test.ts` 的成功路径用例只断言了 `Phase`/`Apply result`，从未断言过 `Known differences` 那一段，所以这个 bug 完全没有测试能抓到。这正是"Story 4.6 只看自己接的口子是不是类型正确、没意识到复用的函数体内部悄悄绑死了 OMP 语义"——单个 Story 的 review 各自都测过自己直接负责的部分，没有一次测过"两边合到一起、真的物化成功之后打印出来的东西对不对"。
- **【已验证属实】裸 `configs status`（不带 planId）永远只会去找 OMP 的 active plan。** `application/launch.ts:370` 硬编码 `findActiveForClient('omp')`，这是 Epic 1 就有的既有行为（当时只有一个客户端，硬编码合理），Story 4.6 引入真实可用的 Claude 客户端后没有同步更新这一条兜底逻辑。**真实后果**：一个只用过 `configs use --client claude-code`、从没碰过 OMP 的用户执行裸 `configs status`，会被告知"未找到"，即便他确实有一个正在活跃的 Claude 启动计划——只有显式传 `configs status <planId>` 才能看到。
- 该 fork 还发现三条较轻的问题：`runClaudeLaunchFlow` 的确认前预览与确认后的 `launchClaudeFresh` 各自独立跑一遍 probe+compile（Story 4.6 自己的 Design Notes 已经承认这是刻意为之的开销，不是意外，但仍造成每次启动尝试真实 spawn 4 次 `claude` 子进程而非 2 次，值得记录）；`claude-launch.ts` 顶部一条设计说明引用"`resolveClientSupport('claude-code')` 被 OMP 测试钉死为 false"作为理由，但 Story 4.6 已经把它翻转成 true，这条注释现在读起来会误导后来者；`cap-fs.ts` 的一条 Design Notes 引用"本仓自身的 `.cap/capabilities/skills/*` 目录"作为证据，但 Story 4.7 已经把 `.cap/` 整个删掉，引用对象不存在了。

## Behavior verification

真实系统行为被反复实际执行验证，不只是靠通过的测试推断：

- **每个 Story 合并后**，协调者独立在自己的工作目录（不信任实现子代理的自报）重跑 `bun run typecheck` + `bun test`，全部 8 次均确认 0 错误、0 回归（`bun test` 通过数从 Story 4.1 后的 330 一路增长到 Story 4.7 后的 444）。
- **Story 4.1**：真实 `claude --help`/`claude --version` 探测（本机 `claude` 2.1.241），非 fixture。
- **Story 4.5**：对真实 `.cap/`（`general`、`agent-assembler` 两个 profile）跑通完整 `loadCapConfigRevisions → compileClaudeAssemblyManifest → compileClaudeAdapterPlan` 链路，逐项比对 `.cap/lock.json`。
- **Story 4.5b**：真实内容物化——把真实 `.cap/` 数据物化进临时目录，断言最终 argv 真实含 `--plugin-dir`（指向真实生成的 `.claude-plugin/plugin.json`）与 `--append-system-prompt`（真实 prompt 正文），当时只验证了 `general`。
- **Story 4.7（Block If 门槛，也是本 Epic 里被验证得最彻底的一次行为）**：协调者对"`agent-assembler` 的真实物化是否真的跑通"这一关键声称，没有止步于信任实现子代理的叙述——在 `.cap/` 被删除**之前**、趁它在协调者自己未合并的主工作目录里依然完整存在，亲自编写并执行一段独立验证脚本，直接调用同一条真实链路。结果：`agent-assembler` 引用的全部 14 个 Skill（含 `lock.json` 声明的 import 型 Skill `grilling`）真实物化成功、`.claude-plugin/plugin.json` 存在、`instructions.appendSystemPromptText` 提取到真实文本、零失败。这是本 Epic 唯一一次"协调者不信任任何一方叙述、完全独立复现"的验证，直接回应了 review 阶段 intent-alignment 视角提出的"diff 无法证明证据真实存在过"的质疑。
- **未做的行为验证**：全 Epic 没有一次真正让 `configs use --client claude-code` 通过 CLI 命令行（而不是直接调用内部函数）触发一次对真实 `claude` 二进制的 fresh spawn 并观察真实进程退出——Story 4.6 的集成测试全部通过 `main()` + 注入的 fake `Claude*Port` 驱动（同 OMP 侧既有测试的一贯写法），从未有一次是"人在终端敲 `configs use xxx --client claude-code`，看着真实 `claude` 窗口/进程起来"的端到端人工验收。这与 PRD/Architecture Spine 定义的"外部开发验收门"（T-1/T-2/T-3 真实任务基线）本就不要求在 Story/Epic 级自动化里覆盖一致，不算缺陷，但记录为"本 Epic 尚未做过的行为验证"，供负责人判断是否需要在合并到 `main` 前手动跑一次。

## Previous-retro follow-through

三个来自更早 epic、状态非 `done` 的 action item：

- **`epic-1-retro-item-6`**（`epic: 1`，跨会话/跨 agent 可追溯性与 sub-agent 超时信号）：**未落地，且本 Epic 提供了新的、更直接的复现证据**——见下方 Findings 的"子代理停滞模式"一节：Story 3.3、4.2、4.4 的实现子代理均出现过"停在等待一个不会到来的通知"而需要协调者手动 `SendMessage` 唤醒的情况，Story 3.2/4.5b/4.6/4.7 还额外出现子代理派生的孙代理试图用 `"general-purpose"` 这个 agent *类型名*（而非可寻址的会话名/id）回传结果、导致消息投递失败、必须由协调者转发的情况。item-6 建议的两项改进（(a) 跨会话决策点留可追溯记录、(b) 更清晰的超时/重试信号）在本 Epic 里，(a) 由协调者手动坚持做到（每次合并都有说明性 commit message），但 (b) 依然完全没有工具化——协调者每次都是凭对话记忆、手动判断"这条消息应该是最后一轮"再决定要不要唤醒，没有任何系统性超时检测。**建议状态：`open`（无变化，证据加强，不建议改 `done`）。**
- **`epic-2-retro-item-1`**（`epic: 2`，`epic-2-context.md` 陈旧措辞）：与 Epic 4 无关的文件，本 Epic 未触碰。**无证据，无法判断是否落地，建议状态不变。**
- **`epic-2-retro-item-2`**（`epic: 2`，发布类 workflow 缺少 `gh` 参数正确性防呆）：与 Epic 4 无关。**无证据，建议状态不变。**

`epic-1-retro-item-7`（CAP 长期兜底该不该开新 epic 处理）已经是 `done`（`resolved_ref` 指向 2026-08-23 的 correct-course，即 Epic 4 本身的立项依据）——不是本次回顾要处理的对象，仅作背景说明：Epic 4 本身就是对这一条的完整回应，且已被本 Epic 彻底交付（`.cap` 已退役）。

## Action items

| # | 发现 | 处置 | 上游教训 | Owner |
|---|---|---|---|---|
| 1 | `getLaunchStatus` 对 Claude plan 复用 OMP 专属 `computeKnownDifferences`，导致成功启动的状态展示自相矛盾（"applied" 与"未物化"同时出现） | **已修复**（第一轮 fix outcome，见第 101～110 行） | 复用一个"类型签名客户端中立"的函数时，要检查它的**函数体**是否真的中立，不能只看签名；Story 4.6 review 阶段的 4 个 lens 都是对着自己新增的 diff 看，没有一个把"复用来的旧函数在新场景下的真实行为"当作检查对象 | control-plane maintainer |
| 2 | 裸 `configs status`（无 planId）硬编码只找 OMP 的 active plan，纯 Claude 用户看不到自己的启动状态 | **已修复**（第一轮 fix outcome，见第 101～110 行） | Epic 1 时代"只有一个客户端"的硬编码假设，在 Epic 4 引入第二个真实客户端后没有被系统性排查（对比：所有新写的 Claude 代码都有意识地避免硬编码，但没人回头检查旧代码里这类假设） | control-plane maintainer |
| 3 | `materialized/` invocation 目录在首次回顾时从未被清理，每次真实 fresh 启动都会留下孤儿目录 | **已修复**（第二轮 fix outcome：新增 `ClaudeInvocationDirPort.cleanup`，并在 `launchClaudeFresh` 的 `finally` 无条件调用；见本文件第 116～122 行及 action item 13） | 一个"已知缺口"被连续 3 个 Story 的 spec 各自记录一次，却没有一次真正变成待办——残留风险记录本身需要一个"谁来收口"的机制，不能只是不断复述 | control-plane maintainer |
| 4 | `application/claude-launch.ts` 直接调用 `content-materializer.ts` 的具体实现（真实 IO），跳过了架构图要求的 port 间接层；该文件其余每个副作用协作者都走了端口 | **已修复**（第一轮 fix outcome，见第 101～110 行） | 无 | control-plane maintainer |
| 5 | `FsLaunchContextWriter`（OMP）与 `FsClaudeLaunchContextWriter`（Claude）近乎逐字重复，且是本 Epic 唯一一处没有注释解释"为什么不共享"的重复 | **已修复**（第二轮 fix outcome，见第 114～122 行） | 本 Epic 其余重复都有意识地写了"为什么不合并"的理由——这一处漏掉说明本仓在"新增重复代码"这件事上的自我审查纪律不是 100% 覆盖，值得作为写作习惯提醒 | control-plane maintainer |
| 6 | `claude-launch.ts` 顶部设计说明引用"`resolveClientSupport` 被钉死为 false"作为理由，Story 4.6 已翻转，注释现在误导读者；`cap-fs.ts` 的 Design Notes 引用已被 Story 4.7 删除的 `.cap/capabilities/skills/*` 作为"现存证据" | **已修复**（第一轮 fix outcome，见第 101～110 行） | 后一个 Story 改变前一个 Story 依赖的事实时，没有回头巡查前面 Story 里"引用这个事实作为理由"的注释是否需要同步更新 | control-plane maintainer |
| 7 | **（复现 `epic-1-retro-item-6`）** 子代理反复出现"等一个不会来的通知"而停滞，需协调者手动唤醒；孙代理反复用 agent 类型名而非可寻址名回传消息导致投递失败——本 Epic 至少复现 7 次，跨 5 个 Story | **process lesson**（保留为未完成的流程观察，不新增本 Epic 内的代码 action item；由 repo owner 决定是否工具化） | item-6 提的两项改进里，"跨会话留可追溯记录"这次协调者做到了（每次都有说明性 commit message），"更清晰的超时/重试信号"完全没有工具化，只能靠协调者凭经验判断——这不是可持续的解法 | repo owner |
| 8 | `writeFileAtomic`（`content-materializer.ts`）与 `replaceBinary`（`github-release-updater.ts`）重新实现同一套原子写入纪律，前者注释里引用了后者但代码未共享 | **已修复**（第二轮 fix outcome，见第 114～122 行） | 无 | control-plane maintainer |
**未被路由为 action item 的发现**（已确认为有意为之、有文档理由的设计决定，不需要后续处理）：三处哈希函数各自独立实现（均有代码注释论证为什么不共享）；`runLaunchFlow`/`runClaudeLaunchFlow` 结构性重复（Design Notes 已论证是为了不动 ~344 个已锁定的 OMP 测试）；`determineClaudeLaunchTarget` 的 `'fresh'` 分支当前不可达（Story 4.4 已披露的设计取舍）；`ClaudeAdapterPlan`→`ClaudeAssemblyManifest` 改名传播（Story 4.2/4.3 已完整记录并正确执行）；`adapters/omp/` 与 `adapters/clients/claude/` 目录路径不对称（OMP 侧遗留问题，非本 Epic 引入，本 Epic 新代码反而是唯一守住了架构脊柱路径约定的部分）。

### Fix outcome（2026-08-24，责任人批准"当场修"后当场执行）

`fix now` 的 4 项（#1、#2、#4、#6）已在本次回顾定稿后立即修复并验证，commit `0d09a1c`：

- **#1**：`getLaunchStatus`（`application/launch.ts`）改为按 `plan.client` 分流——`'omp'` 保留原 `computeKnownDifferences`；非 `'omp'`（含 `'claude-code'`）不再套用 OMP 专属字符串，改为诚实的 `claude-known-differences-not-recomputed-for-status-view` 标记（AD-21 的真实差异只在 launch 时一次性算出且不持久化，状态查询这个只读投影不应该为了重算而重新 probe/物化）。同时修正了版本探测：`clientVersion` 未显式传入时按 `plan.client` 选择 `ompPort`/`claudeProcessPort`，而不是无条件调用 `ompPort.detectVersion()`。
- **#2**：新增 `findMostRecentActivePlan`（`application/launch.ts`），裸 `configs status` 现在跨 `domain/client.ts` 新增的 `ALL_CLIENT_IDS`（`omp`/`claude-code`/`codex-cli`）取 `createdAt` 最新的计划，而不是硬编码只查 `'omp'`。
- **#4**：`application/ports.ts` 新增 `ClaudeContentMaterializerPort`；`content-materializer.ts` 新增 `FsClaudeContentMaterializer` 实现（包装既有 `materializeClaudeContent` 纯函数，该函数自身单测不受影响）；`claude-launch.ts` 的 `LaunchClaudeFreshDeps` 新增 `claudeContentMaterializer` 字段，调用处改为走注入的端口。`cli/index.ts`（`FullDeps`/`CliOverrides`/`openDeps`）与两处测试 fixture（`tests/application/claude-launch.test.ts`、`tests/cli/tui.test.tsx`）同步接线。
- **#6**：修正 `claude-launch.ts` 顶部设计说明（`resolveClientSupport('claude-code')` 已在 Story 4.6 翻转为 `true`，不再是"被钉死为 false"）与 `cap-fs.ts` 的 `resolveSkillSourceRef` Design Notes（明确 `.cap/capabilities/skills/*` 已在 Story 4.7 退役，证据是历史时点的，现在靠 `tests/fixtures/cap-sample/` 复现同一形状）。

**验证：** `bun run typecheck` 干净；`bun test` 444/444 通过（较回顾记录时的基线数量不变，无回归、无新增用例——本次是修复+接线，不新增测试场景）。`sprint-status.yaml` 对应 4 个 action item（`epic-4-retro-item-11/12/14/15`）状态更新为 `done`。

**仍未处理（维持回顾时的处置）：** #7（子代理停滞模式）仍为 `process lesson`——未工具化，且不适用代码修复：该发现描述的是协调者（本会话）自身的多代理编排行为，不是本仓代码库里的缺陷，没有可提交到这个仓库的代码改动能"修复"它；保留为跨 Epic 累积证据，留给 repo owner 决定是否值得投入工具化。

### Fix outcome 第二轮（2026-08-24，责任人要求"现在一并处理"后当场执行）

剩余 3 项（#3、#5、#8，原处置均为 `defer`）已当场修复并验证，随 fix outcome 第一轮一并提交：

- **#3**：`ClaudeInvocationDirPort` 新增 `cleanup(invocationDir)`（`application/ports.ts`），`FsClaudeInvocationDirPort` 用 `rmSync(..., { recursive: true, force: true })` 实现且吞掉异常（best-effort，绝不掩盖真实启动结果）。`launchClaudeFresh`（`application/claude-launch.ts`）把 `invocationDir` 创建之后的全部逻辑包进 `try/finally`，`finally` 里无条件调用 `cleanup`——安全性论证：`ClaudeProcessPort.spawn` 只在子进程真正退出后才 resolve（见 `process-port.ts` 的 `await proc.exited`），所以到达这个 `finally` 的每条路径，要么从未真正 spawn（安全），要么已经拿到 `spawnResult`（子进程已退出，同样安全）。三处测试 fixture（`FakeClaudeInvocationDirPort`：`claude-launch.test.ts`、`tui.test.tsx`、`cli-claude-launch.test.ts`）同步补上 `cleanup` 方法。
- **#5**：新增共享写入原语 `writeJsonLaunchContext`（`adapters/launch-context/fs-json-context-writer.ts`），`FsLaunchContextWriter`（OMP）与 `FsClaudeLaunchContextWriter`（Claude）都改为调用它，各自只保留自己的目录名（`launch-context/` vs `claude-launch-context/`）。此前遗漏：这一项当时没有被实际写进 `sprint-status.yaml` 的 `action_items`（本文档表格里有，脚本调用里被漏掉了）——本轮一并补记为 `epic-4-retro-item-18` 并直接标记 `done`。
- **#8**：新增共享原子写入原语 `writeToSameDirTempFile`（`adapters/system/atomic-write.ts`，同目录临时文件 + 由调用方自行 `rename`）。`content-materializer.ts` 的 `writeFileAtomic` 与 `github-release-updater.ts` 的 `replaceBinary` 都改为调用它。两者的临时文件命名策略保留原样而非强行统一：`content-materializer.ts` 用默认的 `pid+timestamp` 后缀（多个引用可能在一次启动内并发写入，需要唯一性）；`replaceBinary` 显式传入固定的 `.download` 后缀（`tests/adapters/self-update.test.ts` 依赖这个具体路径来模拟写入失败，强行统一会让该用例失效）。两者之间真正不同的部分（`replaceBinary` 在写入与最终 rename 之间还有一次"重命名旧文件为备份"的步骤）保留在各自调用方，未被强行合并——合并会在备份之前就覆盖旧文件，属于真实行为回归，不是这次要做的重构。

**验证：** `bun run typecheck` 干净；`bun test` 连续 3 次运行均 444/444 通过（无回归；此前有一次孤立的 1 fail，重跑 5 次后未再复现，判定为与本次改动无关的既存 flake，未继续追查）。`sprint-status.yaml` 对应 3 个 action item（`epic-4-retro-item-13/17/18`）状态更新为 `done`。

**结论：** 本 Epic 回顾记录的全部 8 个 action item 中，7 个已修复并验证（#1/#2/#3/#4/#5/#6/#8），1 个（#7）确认为流程层面的观察记录、无适用代码修复，维持 `process lesson` 状态供 repo owner 后续决策。

## Acceptance verdict

**verdict: accepted-with-open-items**（criteria: profiled——`epics.md` 的 Epic 4 小节没有一条"整个 Epic 级"的统一验收标准，只有 8 个 Story 各自的 Given/When/Then；本判断是从 8 个 Story AC 逐条核对后综合 profile 出来的，不是对着一条 Epic 级声明打勾）。

**完整性门：通过。** `pending_stories: []`，全部 8 个 Story（4.1～4.7，含 4.5b）均 `done`，不触发强制 `rejected`。

**核心验收标准逐条核实结果（均达成）：**
- Claude Code adapter 走完整的 probe→plan→launch/interpret 窄端口，capability 状态固定四态且绑定真实证据——Story 4.1～4.4 的验收标准，Spec-to-implementation reconciliation 子代理已逐条核实真实代码与 spec 声明一致。
- 内容真正交付（AD-21），不再只是 3 个硬控制 flag——Story 4.5b 的核心承诺，被协调者亲自独立复现验证（`general`、`agent-assembler` 两个真实 profile 全部真实物化成功）。
- `configs` CLI 有真实可用入口——Story 4.6 交付，`resolveClientSupport('claude-code')` 真实翻转，`runClaudeLaunchFlow` 真实接入 dispatch。
- `.cap/` 已安全退役——Story 4.7 交付，两项 Block If 门槛（真实烟雾 parity、运行时依赖核实）均在删除前真正验证通过（非仅叙述），`.cap/` 目录确认不存在，openspec 归档范围逐条核实无误。

**为什么仍是 `accepted-with-open-items`：** 跨 Story 边界 review 发现的 2 个真实、用户可感知的状态展示/查询缺陷（成功启动时的"applied"与错误"未物化"并列；裸 `configs status` 对纯 Claude 用户报告"未找到"）已由 fix outcome 修复并验证。当前保留项是第 7 项流程层观察：子代理停滞/错误寻址模式未工具化，且不适用本仓代码修复；这不影响已独立核实的装配、内容物化和 `.cap/` 退役核心承诺。

**为什么不是 `rejected`：** 完整性门通过；当前保留的第 7 项是流程层观察，不是“装配/交付机制本身失败”——真实的 Skills/Instructions 物化、真实的 `--plugin-dir`/`--append-system-prompt` 交付、真实的 `.cap/` 安全退役，这些 Epic 的核心承诺均已被独立证据确认成立，没有一条 Block-worthy 的发现指向“这个 Epic 实际上没有做到它说要做到的事”。

## Open questions

- **已完成的 action item：** #1/#2/#3/#4/#5/#6/#8 均已在本回顾 fix outcome 中修复并验证；不再把它们列为待处理问题。
- **`epic-4` 状态：** 8 个列出 Story 全部 `done`，且 `epic-4-retrospective` 已完成；按 `sprint-status.yaml` 的状态定义已收口为 `epic-4: done`。`accepted-with-open-items` 只表达回顾保留的流程观察，不覆盖或删除未完成 action item。
- **item-6（子代理停滞模式）要不要做成正式的工具化改进？** 本次回顾提供了本 Epic 内至少 7 次复现的具体证据，比 Epic 1 回顾时"未经证实的过程输入"扎实得多；是否值得投入去做一个真正的超时/重试信号机制，是留给负责人的产品/工具决定，不是这份回顾能替负责人拍板的。
