---
epic: 1
date: 2026-08-22
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 1 Retrospective — 查看、选择并使用 OMP 配置

> 本次运行以委托任务方式执行（无实时交互用户在场确认），因此按 headless 惯例处理：所有本应由人类确认的选择（Epic 选定、验收判定、拟议行动项）都记录在下方 Assumptions 一节，而非默默采纳。

## Epic summary

- **Epic**：Epic 1「查看、选择并使用 OMP 配置」（`_bmad-output/planning-artifacts/epics.md`），MVP 唯一 Epic。
- **Stories**：
  - Story 1.1「查看与比较配置内容」— PR #127（已合并，commit `886f0ce`）
  - Story 1.2「选择配置并使用 OMP」— PR #128（已合并，commit `f155405`）
- **`detect-epic --epic 1`**：`story_count: 2`，`done_stories: [1-1-查看与比较配置内容, 1-2-选择配置并使用-omp]`，`pending_stories: []`（完整性门通过），`retro_key: epic-1-retrospective`，`retro_status: optional`。
- **Diff range**：`886f0ce^..f155405`（= `e2a21c7..f155405`），纯新增（无删除），`packages/control-plane/` 40 个文件，5483 行新增。`git_evidence.py` 的逐提交 `stories` 归属为空数组（提交主题未含精确 id 字符串"1-1-查看与比较配置内容"/"1-2-选择配置并使用-omp"，只含"Story 1.1"/"Story 1.2"字样，脚本按 id 精确匹配未命中）——按 PR 元数据（各 PR 单一提交、PR 正文自述范围）人工核实归属，非脚本自动归属，记录该窄化。
- **证据清单**：
  - Epic spec / Story spec：可读，`_bmad-output/planning-artifacts/epics.md`、`_bmad-output/implementation-artifacts/spec-1-1-*.md`、`spec-1-2-*.md`（含各自 Spec Change Log，记录了 review 发现与修正）。
  - Sprint status：可读，`epic-1: in-progress`（与两个 Story 均 `done` 不一致，见下方"Sprint-status 更新"）。
  - 先前 retrospective：不存在（Epic 1 是当前唯一 Epic，无更早 Epic 的 action items 可供追踪）。
  - Session logs：**不可用**。两个 Story 分支（`origin/feature/control-plane-view-compare-configs`、`origin/feature/control-plane-select-and-use-omp`）在合并前各自只有一个提交（squash 风格），PR #127/#128 均无 review/comment 记录（`gh pr view --json comments,reviews` 均为空数组）。仓库内没有其他会话转录/工作记录文件覆盖这两个 Story 的构建过程。因此"构建期间发生了什么"这一类过程性问题只能依据任务简报中的转述与 spec 文件自身的 Spec Change Log 交叉核对，**不能**从 git/PR 证据独立证实或证伪构建会话内部的具体交互细节（见下方 Findings 的 process-lesson 部分，范围已按此收窄）。

## Findings

### Aggregate views

**Architecture delta** — 全新六边形模块（`domain/application/adapters/cli`），未发现违反既有边界的跨层依赖；`domain/` 确认不 import Bun/SQLite/fs（`packages/control-plane/src/domain/*.ts` 头部检查）。未见引入循环依赖或跨包耦合（`packages/control-plane` 是 workspace 内新增包，`package.json` 只新增 `workspaces` 字段）。

**Duplication map** —
- 一处跨 Story 重复逻辑（`cli/index.ts` 对 `succeeded`/`degraded` 是否可切换的判断与 `application/launch.ts` `transitionLaunchPlan` 内部判断重复）已在 Story 1.2 build 期间的第二轮独立 review 中发现并修正（spec-1-2 Spec Change Log 条目 3，`runLaunchFlow` 改为完全委托领域层判定，仅在 `InvalidTransitionError` 时回退）。已用 `grep` 核实修正确实落地：`src/cli/index.ts` 不再自行判断 `active.phase === 'succeeded' || active.phase === 'degraded'` 作为切换前置条件。**接受为已解决**，不重复记录为待办。
- **一处尚未解决的重复**（Phase 2 adversarial 子代理发现，已用 Read 核实）：`src/adapters/sqlite/repository.ts:188-199` 的 `factColumns`/`factColumnToFact` 与 `src/adapters/sqlite/launch-repository.ts:39-50` 的同名逻辑各自独立实现了同一套 `Fact<T>` ↔ `(status, value, reason, observedAt)` 列的序列化/反序列化，Story 1.1 与 Story 1.2 两次分别写出。未来任何一侧的序列化修正（如值的 stringify 分支）都可能只改一处，另一处静默保留旧行为。**Fix-now**，见 Action Items。

**God-class / size growth** — 按 `git diff --numstat` 净增行数排序：`adapters/sqlite/repository.ts`（409）、`application/launch.ts`（398）、`cli/index.ts`（379，跨两个 Story，commit_count 2）、`cli/render.ts`（282，跨两个 Story）、`domain/activation.ts`（273）、`adapters/sqlite/launch-repository.ts`（241）、`domain/config.ts`（234）。均在六边形单一职责边界内（repository/application-service/CLI-dispatch 各自为一文件），未见单文件承担多重不相关职责的迹象；`cli/index.ts` 和 `cli/render.ts` 因两个 Story 共享同一 CLI 入口/渲染模块而跨 Story 增长，符合预期（Story 1.2 依赖 Story 1.1 的展示字段口径，epic-1-context.md 已声明此依赖）。未发现需要立即拆分的 god-class。

**Pattern divergence** — 未发现明显偏离仓库既有 TypeScript/Bun 约定的模式（本包是仓内首个 TypeScript/Bun 包，暂无更早同类包可比对；测试运行器、`tsconfig.json`、事实值 `Known<T>`/`Unknown` 约定在两个 Story 间保持一致）。

**Spec-to-implementation reconciliation** — 抽查 epics.md 中 Epic 1 的若干验收条件与代码逐一核对，含"不自动选择/排序/隐藏"（`defaultMarker` 用 `Fact<boolean>` 而非裸 `boolean`，未知按 `Unknown` 而非默认 `false` 渲染，spec-1-1 Spec Change Log 已记录该修正）与"resume 完全不拦截"（`process-port.ts` 新增 `DENYLISTED_FORWARDED_ARG_TOKENS` 拒绝 `-c/--continue/-r/--resume/--session-dir` 转发，已用真实 `omp --help` 逐条核实存在，spec-1-2 Spec Change Log 条目已记录并在本次 `grep` 中确认落地）——两处均与代码一致。未做全量逐条核对（11+7 条验收条件全集），按此记录窄化范围。

### Diff-scope review (bmad-review: adversarial / edge-case-hunter / verification-gap)

三个 lens 在隔离子代理中并行运行，各自独立读取 diff range `886f0ce^..f155405` 与相关源文件/测试，重点权重 Story 1.1/1.2 边界。adversarial 11 条，edge-case-hunter 6 条，verification-gap 3 条，共 20 条原始发现；按同一根因合并去重后为 11 组，逐条给出 disposition。多个 lens 独立命中同一根因视为信号增强，已在下方标注。

1. **[跨 Story 边界，defer] Story 1.2 从不检查 Story 1.1 的 `revision.availability`（`Fact<'resolved'>`）就允许启动确认/启动** —`application/launch.ts` `prepareLaunchPlan`/`launchOmp` 只检查修订"是否存在"，从不检查其"是否已机械确认可解析"。命中 lens：adversarial + edge-case-hunter（独立收敛）。`cap-fs.ts` 会把未解析成功的 profile 标为 `Unknown('not-resolved', ...)`，但这一事实从未被 Story 1.2 读取。不违反已宣告的验收条件（AC 未要求阻断 Unknown 可用性的启动），但确是本 Epic 内 Story 1.1→1.2 数据契约未被消费的真实缺口。
2. **[跨 Story 边界，fix-now] `launchOmp` 内部重新拉取修订详情时未捕获 `ConfigNotFoundError`/`ConfigUnsupportedError`** — 同一文件里 `prepareLaunchPlan`/`getLaunchStatus` 都对这两个 Story 1.1 类型化错误做了 catch→typed-failure 处理，唯独 `launchOmp`（确认后、真正 spawn 前的重新拉取）没有，若修订在"用户看到确认提示"与"用户按下确认"之间的等待期被删除/变为 schema 不支持，会导致该错误未被捕获直接向上抛出，绕过本 Story 自己统一的 `renderLaunchFailure` typed-failure 渲染路径。**命中 lens：adversarial + edge-case-hunter + verification-gap，三方独立收敛**，且 verification-gap 额外证实：现有测试的 fake repository 从不抛错，因此这条路径也从未被测试覆盖过。信号强，建议列为 fix-now。
3. **[跨 Story 边界，defer，与 #2 同根因] `getLaunchStatus` 缺少"修订在 plan 创建后变得不可解析"场景的测试覆盖** — verification-gap 独立发现；与 #1/#2 是同一类"Story 1.1 修订状态可以在 Story 1.2 的时间窗口内变化，但后者未必处理"问题的另一个入口，可与 #2 一并修。
4. **[跨 Story 边界，fix-now] `parseCapabilityJson`（Story 1.1）不校验 `skill.name` 非空字符串，`buildOmpArgv`（Story 1.2）直接信任并拼进真实 `omp --skills` 参数** — 命中 lens：adversarial（数据完整性）+ edge-case-hunter（skill 名含逗号会拆分/合并）+ verification-gap（独立证实：`buildOmpArgv`/`BunOmpProcessPort` 从未在测试中被"经由仓储 JSON 往返序列化产出的 revision"驱动过，所有测试都手写 `CapabilityReference` 对象，`tests/integration/cli-launch.test.ts` 也永远用 Fake OMP port，真实 adapter 链条从未在集成测试里被触发）。**三个 lens 独立收敛在同一条 Story 1.1→1.2 数据信任边界上，是本次 Phase 2 里信号最强的一条**，建议列为 fix-now。
5. **[已核验为设计意图，accept-as-is] `renderConfirmationSummary` 不显示 `availability`/`scopeBoundary`/Hooks/Plugins 明细** — 命中 lens：adversarial + edge-case-hunter。已对照 `epics.md` 验收条件原文核对：确认摘要的设计意图明确是"简洁显示…并提供按需展开详情"，不要求把 `configs show` 的全部字段都塞进一次性确认屏。判定为**符合既定设计**，不是缺陷，接受现状。
6. **[defer] 两个 `bun:sqlite` 连接（Story 1.1 的 `SqliteConfigRevisionRepository` + Story 1.2 的 `SqliteLaunchPlanRepository`）都未设置 `PRAGMA busy_timeout`** — 命中 lens：adversarial + edge-case-hunter。MVP 是单操作者本地 CLI，并发窗口小，严重度低，记为 defer。
7. **[已对真实 omp 二进制源码核验，判定不成立，dropped] `findDenylistedForwardedArg` 被指"只匹配 `=` 前的 token，漏掉 `-rSESSION123` 这种无分隔符的短选项拼接形式，可绕过 resume 拦截"** — adversarial 提出。本次**没有只信 lens 的推测**，而是直接读取本机真实安装的 `omp`（17.4.1）源码验证：`~/.bun/install/global/node_modules/@oh-my-pi/pi-coding-agent/src/cli/args.ts` 与 `flag-tables.ts` 显示，短选项（`-r`/`-e`/`-c` 等）在 `OPTIONAL_VALUE_FLAGS`/`STRING_VALUE_FLAGS` 表中按**精确 token 匹配**（`OPTIONAL_VALUE_FLAGS.has(arg)`），`=` 拼接语法仅对 `--` 双横线长选项生效（`args.ts:179`：`arg.startsWith("--") && arg.includes("=")`）；真实解析器不是 getopt 风格的短选项拼接/捆绑。因此 `-rSESSION123` 这个 token 不会被 omp 自己的解析器识别为 `-r SESSION123`，不构成 lens 所称的 resume 拦截绕过。**这是一条被合并调查推翻的发现**，按"发现的来源站不住脚就丢弃，不路由"处理，不计入 Action Items。
8. **[accept-as-is，已有文档化理由] `sanitizeProfileName` 的 hash 后缀不做全局唯一性校验，只降低碰撞概率** — Design Notes 已明确声明"不要求密码学强度"；概率极低且已有既定理由，接受现状。
9. **[defer] `configs switch` 把 `requestConfigSwitch` 抛出的任何 `InvalidTransitionError` 都统一吞掉、回退到新建 plan，未按具体原因区分** — adversarial 独立发现，真实但非阻断（掩盖的是"状态机被意外破坏"这类本不该发生的情况，而非当前已知路径）。
10. **[已在构建期讨论并明确拒绝修正，accept-as-is] `getLaunchStatus` 硬编码查询 `client='omp'`** — adversarial 独立重新发现了这一点，但 `spec-1-2-选择配置并使用-omp.md` Spec Change Log 已记录"不采纳（reject）：`configs status` 硬编码查询 `client='omp'`（MVP 唯一支持 client，语义上不可能有其他 client 的 active plan）"——这是构建期已经讨论并留痕的决定，不是被遗漏的新发现，归为 accept-as-is，不重复开待办。
11. **[fix-now，已用代码直接核验] `readYesNo`（`cli/confirm-prompt.ts`）在 stdin 提前 EOF（如从 `/dev/null` 管道输入，或非交互式调用且未传 `--yes`）时永久挂起** — edge-case-hunter 发现，本次已直接读取 `packages/control-plane/src/cli/confirm-prompt.ts` 源码核实：`readLine()` 只注册了 `stdin.once('data', onData)`，没有注册 `'end'`/`'error'` 监听——stdin 提前关闭时这个 Promise 永远不会 resolve/reject，`configs use`/`switch` 会挂起而不是快速失败。这是一个**已独立核实为真实存在的 bug**（不是推测），在自动化/CI/管道式调用场景下会造成挂起而非明确失败，建议 fix-now。与之相关的"单 chunk 读取可能丢失多行输入"（edge-case-hunter 同时提出）在 spec-1-2 Spec Change Log 中已被构建期 review 讨论并明确 reject（"真实终端/管道单行 y/n 输入极不可能跨 chunk 拆分"），归为 accept-as-is，不重复开待办；但 EOF 挂起是另一个未被那次讨论覆盖的独立问题。
12. **[defer，minor] `prepareLaunchPlan` 在修订解析失败时，`LaunchPlan.configName` 落回未解析的原始 `revisionId` 字符串，没有区分标记** — adversarial 发现，低严重度，可能在调试/展示时造成误导。

### Behavior check

- `bun install`：`packages/control-plane/` 依赖已就位，无变更（真实环境已装好）。
- `bunx tsc --noEmit`：干净退出，0 错误 —— 与两个 PR 正文自述的"干净"一致，本次为独立重新执行验证，非转述 PR 正文。
- `bun test`：**150 pass / 0 fail / 429 expect() calls，13 个测试文件** —— 与 PR #128 正文自述的"150 pass / 0 fail / 429 expect() calls"完全一致，独立重新执行确认。运行过程中 `tests/omp/real-omp-smoke.test.ts` 检测到本机确实安装了真实 `omp` 二进制（`Bun.which('omp')` 非 null），因此该文件的两个用例走的是真实 spawn 路径而非"未安装即跳过"分支；测试仍全部通过，证明 Story 1.2 构造的真实 argv（`--profile`/`--no-extensions -e <path>`/`--skills`）确实被本机真实 `omp` 二进制接受（exit 0），而非仅在 mock/CI 环境下被验证过。

## Previous-retro follow-through

Epic 1 是当前 `sprint-status.yaml` 中唯一的 Epic，`action_items` 键此前不存在（文件当前只有 `development_status`，无 `action_items` 数组）。**无更早 Epic 的 action items 可供追踪** —— 这是"没有可追踪项"而非"文件不可读"：`sprint-status.yaml` 本身可正常解析（`detect-epic` 已成功返回），只是尚未有任何 retrospective 写入过 `action_items`。

## Candidate incidents from the task briefing — verification results

以下四条来自任务简报的"候选事件"，逐条给出本次核验到的真实证据与结论：

1. **构建会话技术栈冲突（Story 1.1，最初被带偏到 Python 约定）**：**无法从 git/PR 证据独立证实或证伪**。`origin/feature/control-plane-view-compare-configs` 分支在合并前只有一个提交（`886f0ce`），PR #127 无 review/comment 记录，仓库内无该 Story 构建期间的会话转录文件。`_bmad-output/implementation-artifacts/epic-1-context.md`（构建前已生成的规划产物）已明确写死"外部 TypeScript/Bun CLI，六边形模块化单体"，`spec-1-1-*.md` 的 Spec Change Log 从 Spec 冻结后的第一条记录开始就已经是 Bun/TypeScript 实现细节，未见任何 Python 相关的往返记录。**结论**：即使该纠偏确实发生过，它发生在"送审 diff 构造之前"，因而在本仓任何合并后的证据（提交、PR、spec 变更日志）中都不会留痕——这是委托式/单会话打磨后再统一提交的开发方式的已知盲区，而非这次事件本身被验证为假。记为**未经证实的过程输入**，不作为已确认事实写入下方 Action Items，但作为过程教训单独记录（见下）。
2. **卡住/错投的子代理报告（Story 1.2，等待补丁应用通知但陷入停滞）**：同样**无法从 git/PR 证据独立证实或证伪**，原因同上（单提交、无 comment、无会话日志）。但 `spec-1-2-*.md` 的 Spec Change Log 确认了大量补丁应用轮次真实发生且真实验证过（"133/133 通过，从 130 增至 133"→"150/150 通过，从 133 增至 150"，两轮独立 review + 多次 `bunx tsc --noEmit` + `bun test` 重新执行的记录），且本次会话已用 `grep` 独立核实了 review 记录中提到的具体代码修正（`DENYLISTED_FORWARDED_ARG_TOKENS`、`runLaunchFlow` 重构）确实存在于当前代码中，`bun test` 独立重跑也确认 150/150 通过——**最终产出的补丁确实全部被应用且被验证**，与任务简报"PR #128 显示补丁最终确实应用"的判断一致。是否存在过程中的停滞和人工恢复，本次证据不能确认，但**最终质量未受影响**这一点有独立证据支持。
3. **CI/治理摩擦（PR #129 破坏两个 legacy CI 门禁）**：**已用真实 CI 运行记录逐条证实**。`gh api repos/zaurakworks/agent-system/commits/<sha>/check-runs` 显示：提交 `3ccf4bf`（PR #129 首个提交）的 `checks` job 中 "Smoke packaged CAP entrypoint" 步骤失败，日志原文 `explain 失败：profile: error: capability lock drift detected; run profile lock after reviewing changes`（capability-lock 漂移检测，因 `AGENTS.md` 内容是被跟踪的 capability 输入）；同时 `windows-assembly`/`macos-assembly`/`posix-assembly` 三个 assembly job 也因此失败。下一提交 `3022b38`（"修复：AGENTS.md 权威声明改动后重新生成 .cap/lock.json"）修好了 assembly 三个 job（全部转 success），但 `checks` job 仍失败——日志显示这次失败点变成 "Check federated entry contract" 步骤（`scripts/test_federated_entry.py`）。再下一提交 `4b59103`（"修复：恢复联邦式入口章节标题以通过声明式投影校验"）后全部 8 个 check 转 success。**与任务简报完全吻合**：两个独立 legacy 门禁（capability-lock 漂移 + 声明式联邦入口标题契约）先后在同一 PR 内被发现并各自修复。
   - **Story 1.1/1.2 自己的 PR 是否付过这个 CI 摩擦成本**：**是，但没有失败**。`gh pr view 127/128 --json statusCheckRollup` 显示两个 PR 都完整跑了 `checks`/`conformance`/`validate`/`test (ubuntu/windows-latest)`/`windows-assembly` 等全部 legacy Python 门禁并全部 `SUCCESS`——PR #130（"CI：legacy Python CAP 检查按路径过滤"，在 #128 之后、#129 之后合并）的正文也明确写"Story 1.1/1.2（#127、#128）两个 PR 已实测验证：只改 `packages/**` 从不触碰 `.cap/lock.json`"。也就是说，Story PR 承担了这些门禁的**运行时间成本**，但因为改动范围（只碰 `packages/**`）天然不触碰 `.cap/lock.json`/联邦入口章节标题这两个契约的输入，所以从未因此失败；`paths-ignore` 过滤器（PR #130）是在这两个 Story PR 都已合并之后才加上的优化，对它们本身没有影响。
4. **CAP 双重职责的深层未解张力**：**已用真实配置文件证实**。`.cap/manifest.toml` 当前仍声明 `runtime = { omp = ".cap/runtime/omp.toml", claude = ".cap/runtime/claude.toml" }`——即 CAP 目前仍同时承担（a）OMP 配置管理（已被本 Epic 的 `packages/control-plane` 取代）和（b）Claude Code agent-capability 装配（skills/profile/instructions，未被取代）。`epics.md` 全文核查确认：MVP 明确排除 Claude Code/Codex CLI（`MVP-FR10`/`AR2`：仅保留 adapter port 边界，明确返回 unsupported，不提供占位实现），当前也没有任何后续 Epic 覆盖"取代 CAP 的 agent-capability 装配职责"这件事。**这是真实的、有证据支持的前瞻性风险**，按下方 Open questions 记录，不在本次retrospective 中处理。

## Action items

1. **[fix-now，代码] `launchOmp` 缺少对 `ConfigNotFoundError`/`ConfigUnsupportedError` 的类型化 catch** —`packages/control-plane/src/application/launch.ts`，`launchOmp` 内重新拉取修订详情的调用点，用与同文件 `prepareLaunchPlan`/`getLaunchStatus` 相同的 try/catch 模式包裹，转成 `applyFailure` + typed-failure 渲染而非让错误裸抛。三个独立 review lens（adversarial/edge-case-hunter/verification-gap）收敛到同一根因，并证实该路径当前无测试覆盖。**owner**：下一次 control-plane 维护/小修迭代；**disposition**：fix-now，作为普通 bugfix 提给正常开发循环，本 retrospective 不代为修改代码。
2. **[fix-now，代码] `parseCapabilityJson` 未校验 `skill.name` 非空字符串，`buildOmpArgv` 从未被"经真实仓储 JSON 往返"的 revision 驱动过** — `packages/control-plane/src/adapters/sqlite/repository.ts`（`parseCapabilityJson`）与 `src/adapters/omp/process-port.ts`（`buildOmpArgv`）；三个 lens 独立收敛，是本次信号最强的一条。建议：(a) `parseCapabilityJson` 增加字段校验，非法数据走既有的 `ConfigUnsupportedError`/降级路径而非信任裸转型；(b) 补一条集成测试，让 `buildOmpArgv`/`BunOmpProcessPort` 真正被"经 `SqliteConfigRevisionRepository` 读回"的 revision 驱动，而不是全部手写对象或永远换成 Fake port。**owner**：下一次 control-plane 维护迭代；**disposition**：fix-now。
3. **[fix-now，代码] `readYesNo`（`cli/confirm-prompt.ts`）stdin 提前 EOF 时永久挂起** — 已直接读代码核实（无 `'end'`/`'error'` 监听）。建议给 `readLine()` 补上 `stdin.once('end', ...)`/`stdin.once('error', ...)`，在提前 EOF 时 resolve 为空字符串（等价于"拒绝"）而不是挂起，让非交互式/管道调用快速失败而不是无限等待。**owner**：下一次 control-plane 维护迭代；**disposition**：fix-now。
4. **[defer，代码] `repository.ts` 与 `launch-repository.ts` 各自独立实现了同一套 `Fact<T>` 列序列化逻辑（`factColumns`/`factColumnToFact`）** — 已用 Read 直接核实两处实现（`src/adapters/sqlite/repository.ts:188-199`、`src/adapters/sqlite/launch-repository.ts:39-50`）确系重复代码，Story 1.1、1.2 各写一份。建议抽成共享的 `src/adapters/sqlite/fact-columns.ts`，两个仓储改为复用。**owner**：下一次 control-plane 维护迭代；**disposition**：defer（非阻断，但典型的"两个独立会话各自实现同一件事"模式，值得下次收尾时清理）。
5. **[流程] sprint-status.yaml 缺少"Epic 全部 Story done 后如何转 Epic 状态为 done"的机制** — `sprint_status.py update` 只支持写 `epic-{N}-retrospective` 键（`--set-retro-done`）与 `action_items`，没有 `--set-epic-status`/类似能力可以把 `development_status.epic-1` 从 `in-progress` 改成 `done`；文件顶部 WORKFLOW NOTES 只描述了"Epic 首个 Story 开始时自动转 in-progress"，从未描述任何自动或半自动转 `done` 的路径。本次 retrospective 因此**没有**、也**不能**把 `epic-1` 改成 `done`（改了就是手改 YAML，`retro-document.md` 明确禁止）。**owner**：负责人/下一次工具迭代——建议给 `sprint_status.py` 增加一个显式、经校验的 `--set-epic-status` 写路径（同样走"读原字节→改→写回校验→失败回滚"的模式），或在文档里明确"Epic 状态本来就要人工确认后手动改"是预期设计而非缺口。**disposition**：fix-now（工具缺口，非本 Story 代码缺陷）；**process lesson**：Epic 级状态转换目前无任何脚本化路径，全靠人工——这本身就是下一次同类收尾工作会重复卡住的地方。
6. **[流程，未经证实，仅作为过程教训输入]** 若"构建会话最初被带偏到 `src/agent_system/` Python 约定后自行纠偏"（候选事件 1）与"子代理补丁报告因地址错误未送达父会话，需人工恢复"（候选事件 2）确有其事，两者共同指向同一类过程风险：**委托式/长时运行构建会话的中间状态（错误的初始假设、子代理通信失败）在会话结束、squash 提交后完全不可追溯**。建议：(a) 对跨会话/跨代理委托的关键决策点（例如收到与已锁定架构冲突的初始简报时）留一条可追溯记录（哪怕只是 commit message 或 PR 正文里的一句话），而不是让它随会话结束消失；(b) 子代理汇报机制的地址/寻址错误应有更明确的超时与重试提示，避免父会话无限期等待一个永远不会到达的通知。**owner**：负责人（关于是否值得为此改变会话记录/子代理寻址约定）。**disposition**：defer（无法在本仓证实具体细节，且已知最终产出质量未受影响；作为观察输入而非确认缺陷）。
7. **[前瞻性风险，非本 Epic 范围]** CAP 的 Claude/Codex agent-capability 装配职责（skills/profile/instructions 装配）目前无任何取代计划或 Epic 覆盖，而 `README.md`/`AGENTS.md` 的"Python CAP 降级为历史资产"声明只覆盖了"产品政策定义"这一面，没有覆盖"CAP 仍是当前 Claude Code/Codex 会话本身如何被装配"这一操作性事实。**owner**：负责人（是否需要新开一个 Epic/规划事项覆盖这块，或明确接受 CAP 在这一职责上继续作为当前实现）。**disposition**：不在本 retrospective 处理，记为 Open questions 的前瞻输入。

## Acceptance verdict

**accepted-with-open-items**

- **Criteria**：declared —— `epics.md`/`spec-1-1-*.md`/`spec-1-2-*.md` 均含明确的 Given/When/Then 验收条件（Story 1.1 共 7 条，Story 1.2 共 11 条）。
- **完整性门**：通过 —— `pending_stories: []`，两个 Story 均 `done`。
- **证据支持**：`bunx tsc --noEmit` 与 `bun test`（150/150）独立重新执行通过，与 PR 正文自述一致，非仅转述；抽查的验收条件与代码一致；已知的 Story 间重复逻辑已在构建期修正并被本次核验确认落地；三个独立 review lens（adversarial/edge-case-hunter/verification-gap）在 Epic 全量 diff 上运行，其中一条声称的安全绕过（denylist 短选项拼接）已对照真实 `omp` 二进制源码核实后判定不成立并丢弃，体现证据优先于 lens 推测。
- **未阻断的 open items**（均不违反任何已宣告的 Given/When/Then 验收条件，因此不构成 rejected 的理由）：
  1. 3 条 fix-now 代码发现（`launchOmp` 缺类型化 catch；`parseCapabilityJson`/`buildOmpArgv` 数据信任边界缺校验与测试覆盖；`readYesNo` stdin EOF 永久挂起）——均为 Story 1.1↔1.2 边界或健壮性缺口，不是验收条件缺失。
  2. 1 条 defer 代码发现（`factColumns` 序列化逻辑跨 Story 重复）。
  3. `sprint-status.yaml` 缺少 Epic 状态转 `done` 的脚本化路径（工具缺口，不影响本 Epic 交付内容本身）。
  4. 两条未经证实的过程性候选事件（构建期栈冲突、子代理寻址失败）作为过程教训记录但未经证实。
  5. CAP 双重职责的前瞻性风险已记录待负责人决定。
  
  这些均不构成"验收条件未满足"或"存在阻断性发现"，因此不构成 rejected 的理由；但也不是"零遗留项"的纯粹 accepted，故为 **accepted-with-open-items**。

## Open questions

- CAP 的 agent-capability 装配职责（(b)，见候选事件 4）是否需要专门的后续 Epic/规划事项覆盖？还是负责人明确接受 CAP 在这一职责上继续作为当前实现，不在近期路线图中？
- `sprint_status.py` 是否应该增加 `--set-epic-status`（或等价机制）？如果负责人认为 Epic 状态就应该保持人工判断、不脚本化，也请明确记录这一决定本身（目前只是"没做"，不是"确认过不需要做"）。
- 若候选事件 1/2 描述的过程细节确实发生过，是否值得改变委托式构建会话的记录习惯（哪怕只是留一行 PR 正文备注），以便下次同类 retrospective 能独立核实？

## Assumptions

（本次以委托任务方式运行，无实时交互用户在场确认，按 headless 惯例记录以下本应由人类确认的选择）

- **Epic 选定**：由调用方在 Skill 调用参数中明确指定 Epic 1，未走自动探测路径；已用 `detect-epic --epic 1` 核实该 Epic 存在且 `story_count: 2`。
- **`pending_stories` 结果**：`[]`（空），未触发"未完成 Epic"确认流程。
- **机器验收判定**：`accepted-with-open-items`，未经人类在本次会话内确认——已在上方"Acceptance verdict"节给出完整证据链，供负责人事后复核/否决。
- **拟议 Action Items**：共 7 条（3 条 fix-now 代码、1 条 defer 代码、1 条工具流程缺口、1 条未经证实的过程教训、1 条前瞻性风险），均为 *proposed*，未被应用/执行，也未被任何人类在本次会话内确认为"应该做"。
- **未运行 Phase 3 团队讨论**：按 skill 定义默认跳过（仅当用户明确要求"as a team"讨论时才触发），本次委托任务未明确要求，因此跳过。
- **Sprint-status 更新范围**：只计划调用 `sprint_status.py update --set-retro-done` 写入 `epic-1-retrospective: done` 与本文档记录的 7 条 action items（均以 `status: open` 写入）；**不**尝试修改 `development_status.epic-1` 本身（工具无此能力，原因见 Action Items 第 5 条）。因为没有更早 Epic 的 action items，本次不传 `--set-action-status`。
