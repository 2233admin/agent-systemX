---
scope: Agent System MVP 的 OMP Agent 配置、激活、验证、观察与跨 Session 追溯
purpose: 面向后续实现者的中文 build substrate
altitude: feature
updated: 2026-08-25T11:42
---

- (constraint) clean-slate：现有 Python CAP 仅可作为 Bad Case 证据，不是需求、架构或迁移基线。
- (constraint) MVP 只支持 OMP Agent 单客户端；不得为未承诺的多客户端能力预置进程协议。
- (constraint) 稳定配置只引用 Instructions、Skills、MCP 与任务期输入入口，不复制任务内容或私域原文。
- (constraint) 系统必须区分期望装配、最终结果、运行观察与 Unknown；提供三层验证与跨 Session 可追溯；普通路径最多一次激活确认。
- (decision) 采用 TypeScript 原生 OMP 插件，领域内核保持纯 TypeScript 并通过 ports 与 OMP 适配器隔离；绑定全部 MVP 能力，防止为单客户端 MVP 引入 IPC、重复类型映射和双进程生命周期。
- (direction) 已比较 TypeScript 原生插件、Go/Rust/Python 独立 CLI 加 TypeScript 适配和纯离线配置编译器；独立 CLI 的隔离收益不足以覆盖单客户端 MVP 的协议与分发成本，纯离线方案无法满足运行观察闭环。
- (decision) 采用 Bun 内置 SQLite 作为本机稳定配置修订、激活、差异、观察、验证证据与替代链的唯一持久权威；可读 JSON/Markdown 仅为可重建投影，禁止反向写入或成为第二权威。该决定绑定 FR-3~14，防止跨文件部分提交、并发漂移和跨 Session 丢失。
- (decision) 每次激活是 session-scoped 单元，绑定不可变稳定配置修订、输入引用与指纹、客户端环境证据；运行观察只追加证据，不能反写期望状态，跨 Session 只续接持久事实而不继承旧授权或动态任务状态。
- (decision) 采用六边形模块化单体：domain 不依赖 OMP、SQLite、文件系统或时钟；application commands 是唯一状态变更入口；OMP、SQLite、投影、时钟与指纹为 adapters，依赖方向只向内；同一 Bun 进程部署，不引入服务或 IPC。
- (decision) 单个原生 OMP 扩展提供三类表面：用户命令仅做选择/一次确认/检查，LLM 类型化工具仅做低频配置修订与证据记录，生命周期钩子只读采集观察；三者统一调用 application，禁止直接访问 SQLite。
- (decision) 激活状态机固定为 prepare→confirm-once→apply→observe/reconcile；必需能力、必需输入、实质变化裁决或证据完整性缺失时 fail closed；仅可选项缺失可 degraded，且必须列出差异、Unknown 与受影响能力。
- (decision) SQLite 事务与 OMP 外部副作用不宣称原子：每次操作使用幂等 operation id，副作用后保存可观察结果；失败记录部分结果与差异，重试先 reconcile，禁止以推测回滚冒充成功。
- (decision) Truth 使用显式 Known(value,evidence) 或 Unknown(reason,observedAt) tagged union；null/false 不表达未知。三层验证分别保存，只有三层均有直接证据且通过才可派生 Verified。
- (decision) SQLite 使用 strict tables、WAL、单写者序列化、乐观修订条件和事务迁移；可读投影采用 allowlist，私域原文、凭据和动态任务内容不得持久化或写入日志。
- (version) 目标栈经 2026-08-22 官方源核实：Bun 1.3.14；@oh-my-pi/pi-coding-agent 17.4.4；@oh-my-pi/omptype 17.4.4；TypeScript 7.0.2。来源：https://github.com/can1357/oh-my-pi/blob/main/package.json
- (constraint) OMP 官方扩展是 Bun 同进程加载的 TS/JS 模块，可注册 commands、tools 与生命周期事件；扩展无沙箱，必须使用受管 timer、捕获边界且不得 detached promise。来源：https://github.com/can1357/oh-my-pi/blob/main/docs/extensions.md
- (direction) 语言评估按运行环境、客户端集成、类型与测试、分发、长期维护比较：TypeScript 与 OMP/Bun 原生类型和 bun:test 同栈且无需 IPC；Go/Rust 二进制分发强但需新增进程协议与类型映射，Rust 跨编译通常还需 linker；Python 迭代快但 venv 不可搬运且仍需解释器/适配层。来源：https://bun.com/docs/bundler/executables、https://rust-lang.github.io/rustup/cross-compilation.html、https://docs.python.org/3/library/venv.html
- (decision) 运行与分发：用户作用域 OMP Marketplace/Git/本地插件；运行在 OMP Bun 进程内，使用本机 SQLite，不设独立服务、IPC 或遥测；安装/升级与每次激活分离。来源：https://github.com/can1357/oh-my-pi/blob/main/docs/marketplace.md
- (decision) FR-2 固定 CandidateSet→Recommendation→UserDecision：默认 2~3、最多 4 个可区分候选，逐项保存来源、价值、适用边界、差异、依据、风险与 Unknown；只有高风险、权限、不可逆或价值取舍升级。
- (constraint) FR-12 首轮验收固定 T-1/T-2/T-3，缺合适 T-3 时以 T-4 替代并记录理由；每任务 1 基线+2 稳定样本且至少 1 个新 Session；最小样本后只接受、调整、停止或为具名 Unknown 追加一次。
- (decision) 事实模型分别表达配置/安装、发现/启用、连接/批准、本次激活、实际调用/生效；差异分类至少包含缺失、额外、禁用、覆盖、预算/限制裁剪及影响。外部来源安全性、可信度、任务适用性不得由已配置自动推导。
- (event) PRD 与 addendum reconcile 完成：补齐 FR-2、FR-5/6、FR-11/12、FR-13、NFR-6 和 addendum 显式非目标；lint_spine 通过。
- (event) spine finalized：PRD/addendum reconcile 完成；rubric、current-tech、divergence、data-safety 四个独立审查修复后均 PASS；最终 lint_spine 0 findings。
- (source) 研究输入:../../research/technical-three-cli-assembly-architecture-2026-08-22/research.md;2026-08-22;外部窄核心 OMP-first、三端能力差异与语言分发证据。
- (decision) AD-2/AD-3 原位修订:唯一组合根改为外部 TypeScript/Bun CLI;OMP 为首个 client adapter 并保留只转发请求/观察的薄 TS bridge;不建 daemon 或三端同步实现。
- (decision) AD-4 保留 SQLite 产品权威:先前用户明确选择继续有效;研究中的轻量文件 launch index 不足以承载稳定修订、验证、样本与 Bad Case;客户端凭据/transcript/Session 仍不入库。
- (decision) AD-7/8/18 修订为 launch-scoped 激活:确认绑定 operation+plan+manifest;实际 Session locator 启动后取得;证据区分 planned/launched/observed/verified;in-session 切换配置必须新 operation 并重启/恢复。
- (decision) AD-13/14/15 修订:同 native Session resume 用本地 lease;CLI/bridge/client 独立故障域;Bun standalone 与 OMP bridge 分发分离,客户端升级先 probe 和 fresh-resume smoke。
- (decision) 新增 AD-19:AssemblyManifest/AdapterPlan/LaunchReceipt 是唯一跨客户端合同;配置 precedence、trust、hook 与 Session 语义不得进入公共领域模型。
- (decision) 语言决定更新:TypeScript+Bun 外部 CLI 采用;Go 仅在 Bun 分发形成连续两个 release 实证硬门时作为第一反转;Rust/Python/纯脚本不采用。
- (version) 更正旧 version 记录:2026-08-22 稍后复核 OMP moving main 已为 packages 18.0.0、packageManager Bun 1.4.0;旧 17.4.4/1.3.14 快照不再作为当前事实。架构删除具体 main 种子,支持版本仅由 release-pinned probe/smoke 决定。
- (decision) Reviewer fixes:AD-16 固定触发类别与工作/证据;AD-7 新增持久 ChangeAssessment;AD-17 固定完整样本比较键与不可变 ValidationDecision。
- (decision) Reviewer fixes:AD-6/9/14 固定 bridge allowlist、secret-free persisted plan、RuntimeLaunchSpec 调用期边界、原子 invocation 工件、端到端关联、幂等 reconcile 入 SQLite 与清理。
- (decision) Reviewer fixes:AD-8/19 拆分 observationStage 与 validationMethod;capabilityId/subject/requiredness、SourceId 完备 disposition、effect 语义固定。
- (decision) Reviewer fix:AD-13 Session lease 持久 ownerOperationId+单调 fencingToken,仅证明进程树结束后回收;否则 locator blocked 或 fresh/fork。
- (event) architecture update finalized:research integrated;AD-1~AD-19 stable;lint 0 findings;rubric/current-tech/divergence/data-safety reviewers PASS after fixes.
- (decision) sprint-change-proposal-2026-08-23-cap-claude-codex-adapter.md 与 prd.md 2026-08-23 裁决更新已获负责人批准:Claude Code 激活为第二客户端(Epic 4)。AD-1 原地追加追溯澄清句(保留原文),不删除'MVP 只实现并验收 OMP'的历史表述,只说明 Claude Code adapter 是 MVP(Epic 1~3)之外新增能力域。
- (decision) Codex 不同批激活:核实 .cap/runtime/ 目录只有 claude.toml,没有 codex.toml,即当前无 Codex 侧真实装配 Bad Case 证据;AD-1、Deferred 小节均标注 Codex 继续 Deferred,等待独立证据。
- (constraint) 负责人明确要求硬控制(宿主原生可强制执行的权限/工具/MCP 边界),排除 prompt 文字类软约束充当 supported 证据;排除 OMP extension 作为实现路径(经 WebFetch 核实 oh-my-pi/docs/extensions.md 不能声明 Skills/Instructions、不能代表独立客户端身份);新 adapter 必须复用 AD-19 窄端口与 capabilityStatus 枚举,不新建平行能力语义。写入 AD-19 的 2026-08-23 MVP 边界更新段。
- (decision) 新增 AD-20:Claude Code adapter 不新增状态机,复用 AD-18 唯一转换表;fresh target 与 OMP 同构(prepared→...→succeeded 等);already-running session target(如本仓自身 Claude Code session)的 apply 只能解析为既有终态 requires-restart,observationStage 重启前不越过 planned——防止把无法验证的热更新冒充 apply 成功,也防止为 Claude Code 另建一套与 OMP 不兼容的平行状态机。取舍依据:AD-18 的 requires-restart 本就是为'无法热应用、需要新进程'设计的通用终态,直接复用比新建变体更收敛。
- (decision) 结构种子新增 adapters/clients/claude/,不新增 adapters/clients/codex/(见 Codex 证据决定)。补充'.cap/ 退役顺序'新小节,固定四步:落地新 adapter→一次性 parity 验证→本仓自身切换→退役 .cap/ 本体;严格按序,不得先退役后设计替代,确保本仓自身 Claude Code session 装配在迁移期间不中断。
- (question) Codex CLI 的会话模型(是否也存在 already-running session 情形、其硬控制原生原语是什么)未知,留待 Codex 自身被独立证据触发激活时判断,不预先假设与 Claude Code 相同(AD-20 open question)。
- (event) architecture update finalized(2026-08-23,Epic 4):AD-1、AD-19 MVP 边界、Deferred 小节原地追加追溯澄清;新增 AD-20;结构种子新增 claude/ adapter 目录;新增 '.cap/ 退役顺序' 小节;能力映射表新增 Epic 4 行;验证边界新增 Claude adapter contract 门。AD-2~AD-18 Rule 正文未改动。
- (question) Fast path，本次为窄范围更新（Epic 4 内容物化机制 + 退役顺序重写），依据 sprint-change-proposal-2026-08-24-cap-retirement-redesign.md（已批准）与 spec-4-5 的 2026-08-24 追加调查证据；不产出额外交付物，只更新 ARCHITECTURE-SPINE.md 本身。
- (decision) 本仓自身正在运行的交互式 Claude Code session 与 .cap 渲染管线无关：其 skills/CLAUDE.md 由 Claude Code 原生项目目录发现机制读取 git 跟踪文件；.cap 真实治理的对象是 cap use <role> --cli claude 新建的隔离 worker/assembler 会话（独立 CLAUDE_CONFIG_DIR，与当前工作目录无关）。据此撤销 AD-20 与 .cap 退役顺序中把'本仓自身 session'当作 .cap 治理对象的既有关联；AD-20 的 already-running/requires-restart 规则本身不改（对任何用户未来发起的'把装配应用到一个已在跑的会话'请求仍适用），只是不再套用到本仓自身维护 session 这个具体场景。证据来源：openspec/changes/archive/2026-08-20-add-claude-cap-adapter/design-spec.md 与本仓磁盘实测（2026-08-24 追加调查，spec-4-5）。
- (decision) OMP 与 Claude Code 两个 client adapter 在内容装配上存在真实的能力非对称，不追求统一实现：OMP 的 buildOmpArgv 只传 --skills <name1,name2> 等抽象名字，交给 OMP 自身的原生 marketplace/profile 机制解析内容，Agent System 从不物化内容（已验证：process-port.ts 现状）。Claude Code 的 --plugin-dir 只接受一个真实文件目录路径，宿主没有'给个名字帮我解析'的原生机制——这是 .cap 的 cap use --cli claude 必须自己生成 <generation>/native/plugin 目录内容、再指给 --plugin-dir 的根本原因，不是 .cap 实现细节的偶然选择。因此 Claude Code adapter 必须承担内容物化，OMP adapter 不需要，两者不是同一套机制的两个实现，而是各自适配所属宿主的真实原生装配语义（AD-19 一贯原则：端口只统一装配意图/能力声明/证据，不统一客户端配置文件语义）。
- (decision) 新增 AD-21：Claude Code adapter 的内容物化只发生在调用作用域内，从不持久化。规则：Claude adapter 的 launch 阶段对装配意图引用的每个 CapabilityReference（Instructions/Skills/MCP），把其 sourceRef 解析为真实内容（读操作，不复制进 SQLite/投影/receipt——AD-6 边界不变），写入既有 Story 4.3 的 ClaudeInvocationDirPort 隔离目录，按类型分别通过 --plugin-dir（Skills）、--append-system-prompt（Instructions）、--mcp-config + --strict-mcp-config（MCP）交付给新 spawn 的 Claude 进程；调用结束（succeeded/degraded/failed/incomplete 任一终态）后连同其余 invocation 目录一并清理，不留存。sourceRef 无法解析为真实可读内容时，该 capability 记为 unsupported（必需）或 degraded（可选），遵循既有 AD-10 fail-closed，不得静默跳过或用占位内容伪装已物化。Binds：AD-6、AD-9（invocation 目录生命周期扩展到内容而不仅是 secret）、AD-19、Epic 4。Prevents：把'内容引用已声明'误当'内容已交付'；为图省事把真实内容写回 SQLite 或投影，破坏 AD-6 内容所有权边界；对无法解析的引用假装物化成功。
- (decision) Story 4.6（CLI 入口）不需要新增架构决定：domain/client.ts 的 resolveClientSupport('claude-code') 从硬编码 unsupported 改为基于 Story 4.1 BunClaudeCapabilityProbe 真实探测结果，configs use/switch --client claude-code 复用 Story 1.2 已确立的确认/生命周期流程与 Story 4.3/4.4 已实现的 fresh/already-running 分支——这是在已有 AD-7/AD-18/AD-19/AD-20 合同内的直接实现，不构成新的不变量。
- (constraint) AD-21 的 sourceRef 解析能力当前受 Epic 3 仍未拍板的'真实数据源接入协议'开放问题限制（epic-3-context.md：真实数据源抓取尚无架构决定）。今天可靠可解析的 sourceRef 主要是经 scripts/seed-from-cap.ts 从本仓 .cap/ 真实文件灌入的修订；Story 4.5b 的实现范围只需覆盖这一类已知可解析来源，其余来源类别遇到无法解析时按 AD-21 走 unsupported/degraded 路径，不need等 Epic 3 数据源问题解决才能交付。
- (decision) 重写 '.cap/ 退役顺序' 小节，四步收窄为三步：(1) 落地新 adapter——probe/plan/launch/interpret 加上 AD-21 内容物化能力与 configs CLI 的真实 Claude 入口（Story 4.1/4.2/4.3/4.4/4.5b/4.6）；(2) 一次性 parity 验证——新 adapter fresh 启动的真实产出（经 AD-21 物化后的 --plugin-dir/--append-system-prompt/--mcp-config 内容）与 cap use <role> --cli claude 的真实产出做真实烟雾对照，不只是静态 manifest 结构比对；(3) 退役 .cap/ 本体——仅在第 2 步验证稳定后执行，移除 .cap/ 目录并归档治理它的 openspec change。删除原第 3 步'本仓自身切换'：已证明其对象不存在，不再作为退役前提。
- (event) Reviewer gate 三路并行：rubric walker、current-tech、adversarial，均 PASS with notes。触发修复：(1) 关键——cap-fs.ts 把 .cap 灌入修订的 sourceRef/contentFingerprint 全部硬编码为 Unknown，AD-21 原'已知范围边界'的前提不成立；改写为阻塞前提，要求 Story 4.5b 先修 cap-fs.ts 写入真实可解析路径（该脚本本就读得到，只是当前丢弃）。(2) 关键——物化目录布局未钉住且与 CLAUDE_CONFIG_DIR/cwd 共享命名空间；改为专用 materialized/ 子目录，Skills 复用 .cap 已实测的 native/plugin/ 真实 Claude plugin 包布局（.claude-plugin/plugin.json + skills/<name>/），不新发明格式。(3) 高——清理时机绑定 invocation 目录既有清理节点（该节点当前对两个 adapter 都未真正实现，记为既有缺口非本条新引入），且不得在 Claude 子进程仍可能读取期间清理。(4) 高——--plugin-dir/--append-system-prompt 未经 Story 4.1 probe 覆盖，仅有文档证据；按 AD-15 原则要求 Story 4.5b 先扩展 probe，且必须重新 probe 而非复用旧版本快照（2.1.236 vs 实测 2.1.241 漂移）。(5) 高——epics.md Epic 4 开篇段落与 Story 4.7 仍写着旧四步顺序/第 4 步/openspec change 措辞，与本次改写的三步顺序矛盾；已同步改写为三步映射，并把 'v3-assembly-executor 等 openspec change' 更正为 'openspec/specs/ 下与 .cap 相关的现存 spec'。全部修复已直接写入 ARCHITECTURE-SPINE.md 与 epics.md，复跑 lint_spine.py 0 findings。中/低优先级发现（AD-9 原子写入未对物化文件显式声明、mcp.json 命名细节）已在 AD-21 Rule 正文中一并写清，不留 tail。
- (event) architecture update finalized（2026-08-24，Epic 4 内容物化 + 退役顺序重写）：新增 AD-21；AD-20 原地追加 2026-08-24 澄清（去掉'本仓自身 session'作为 already-running 例子，规则本身不变）；'.cap/ 退役顺序' 小节四步收窄为三步；能力映射表与验证边界新增 AD-21 对应行；epics.md Epic 4 开篇段落、Story 4.5/4.5b/4.6/4.7 与 sprint-status.yaml 同步更新。lint_spine 0 findings；rubric/current-tech/adversarial 三路 reviewer PASS with notes，全部触发修复已应用。
- (constraint) 2026-08-24 现状核实（Skill 资产面）：仓内 12 个组 / 82 个 Skill 分成两套互不相同的建制。11 个组住 plugins/<组>/skills/<名>/，own 或 vendor，有版本号、有 plugin.json、在两份 Marketplace 里，共 13 个成员；bmad 组 49 个成员（占体积 57.5%），fork 上游 v6.11.0，无版本号、未打包，住 .agents/skills 与 .claude/skills 两份逐字节相同的 git 跟踪副本。证据：tools/skill_registry/registry.md 生成物、diff -rq 两目录无输出。
- (constraint) 2026-08-24 现状核实（装配路径）：两条真实路径，只有一条被设计过。(1) configs 装配——OMP 侧 buildOmpArgv 传抽象名字、宿主自解析；Claude 侧 AD-21 内容物化后经 --plugin-dir 交付；权威是 SQLite 修订。(2) 客户端原生发现——.claude/skills 与 .agents/skills 被 Claude Code／Codex 从 git 跟踪文件直接读取，49 个 bmad skill 全部走这条，不经过 configs、不在 SQLite 里、不产生 receipt，在 ARCHITECTURE-SPINE.md 中无任何 AD 覆盖。AD-20 的 2026-08-24 澄清已认定这条路与本产品渲染管线无关，但未说明它本身算什么。
- (constraint) 2026-08-24 现状核实（声明面失效）：plugins/skill-imports.toml 自称'当前默认装配声明'、plugins/README.md 写着'当前 profile 只按 plugins/skill-imports.toml 显式装配'，但该文件只列 1 条（grilling），且无任何产品代码读取——全仓唯一引用者是一致性测试 plugins/tests/workflow-routing.test.ts。它既不覆盖 49 个 bmad，也不是 configs 的输入。
- (constraint) 2026-08-24 关键证据（自我开发装配面缺失）：entrypoints/agent-system.md 三处强制加载 Skill——第 62 行 orchestrated-collaboration（共享写入前置检查）、第 78/84 行 adaptive-problem-solving（问题求解治理、父目标验收；后者是硬门：未完成检查不得声称产品闭环完成）。三者连同 grilling 都只住 plugins/<组>/skills/<名>/，而 plugins/ 不是 Claude Code 或 Codex 的原生发现路径，因此对本仓会话结构性不可见（本 session 实测：Skill 列表中只有 bmad 49 个与宿主自带项，无这三个）。措辞'加载可用的'把强制门静默降级为空操作。结论：本仓会话拥有的 49 个 Skill 来自 vendor 安装器碰巧写入的路径，而本仓自己撰写并明文强制的 13 个够不着；'本仓 agent 应该有哪些 Skill' 这件事在仓内没有任何载体表达。
- (direction) 2026-08-24 负责人裁决：自我开发装配面（消费者 B）走 configs，即'吃自己的狗粮'。已向负责人明示代价——AD-20 定论 configs 够不到原生启动的会话，且 #173 的供给缺口未解决，等于承诺 clone 后直接敲 claude 是裸的；负责人在知情后仍选择该方向。已比较并放弃：显式声明+生成投影（把双份降为生成物，但让 configs 之外再立一个装配载体）、承认原生发现即事实不设计（等于接受入口三处硬门为空操作）、只钉边界把机制列为 Deferred。
- (constraint) 2026-08-24 关键证据（bmad 字节的真实来源与可复现性）：_bmad/ 磁盘仅 150 KB，被跟踪的只有 _config 清单、两个 module 的 config.yaml、v6-shims、scripts 与 custom。_bmad/_config/skill-manifest.csv 列 49 条、path 指向 _bmad/core/<skill>/SKILL.md，但该路径在磁盘上不存在——安装器只写了 ides: [claude-code, codex, pi] 的 IDE 投影，未保留 canonical 副本。因此 .claude/skills 与 .agents/skills 是这 49 个 skill 字节仅有的两份拷贝，取消跟踪即从仓库删除唯一副本，恢复手段是重跑 BMad 安装器。而 _bmad/_config/manifest.yaml 钉了 installation.version 6.11.0，等价于 lockfile：pin 已被跟踪、字节也被跟踪，形状上等同于同时提交 lockfile 与 node_modules。
- (decision) 2026-08-24 负责人裁决（自我开发装配的落法）：选纯 AD-21 形态——本仓 agent 会话的 Skill 一律经 configs use <rev> --client <client> 启动时物化交付，不给 AD-21 开工作树例外，不把 configs 降格为只做声明校验。据此 .claude/skills 与 .agents/skills 最终退役为非跟踪；bmad 字节由 BMad 安装器按 _bmad/_config/manifest.yaml 已钉的 6.11.0 每机复现。已明示并被接受的代价：不经 configs 启动的会话（含裸敲 claude）没有任何 Skill；该形态在 #173 的供给能力落地前不可用。
- (decision) 2026-08-24 新增 AD-22（自我开发装配与产品装配面共用同一条路径；客户端原生发现不是装配面）[ADOPTED]。五条 Rule：(1) 唯一装配路径——本仓 agent 会话经 configs use 启动、按 AD-21 调用作用域物化交付；客户端原生项目目录发现不产生 manifest/capabilityStatus/receipt，在 AD-8 事实层级里不能声称 observationStage，未经 configs 启动的会话按定义无装配能力（明确接受，非缺陷）。(2) 字节所有权按来源判定不按位置——fork 组字节是上游可复现产物，仓库跟踪 pin 不跟踪字节；own/vendor 组字节是仓库源码，继续跟踪。(3) 组是第一结构——装卸/版本/发现/判定/复核以组为单位（12 组 / 82 Skill：判定 12 次不是 82 次），own/fork/vendor 三分是判据本身，fork 零改动必须机械可验证；该模型此前只在 tools/skill_registry/README.md，本条固定到权威面。(4) 退役顺序三步严格按序，复用 .cap/ 先例。(5) 规则与能力必须同真——仓库规则不得强制加载本仓装配下不可获得的 Skill，'加载可用的 X' 不构成豁免。
- (event) 2026-08-24 Reviewer gate（AD-22）：lint_spine 0 findings；三条 lens 因子 agent 未获授权改按 gate 既定回退顺序执行，各自落盘 reviews/review-ad22-{rubric-walker,current-tech,adversarial}.md（注意：首次写入时误覆盖了上一轮 Epic 4 的 review-adversarial.md 与 review-current-tech.md，已 git checkout 还原，本轮改用 review-ad22-* 前缀）。三 lens 均 PASS with findings，共 1 critical + 2 high + 2 medium，全部 autofix 已应用：[critical] sourceRef 跨机器可移植性语义从未被任何 AD 固定，而 AD-22 的每机复现让绝对路径假定（content-materializer 现状 + AD-21 阻塞前提段）与之直接冲突，两个实现者会各自发明并导致静默全量降级——改为要求退役第 (1) 步一次裁定并同时约束解析侧与供给侧，不预选方案但禁止各自发明；[high] 组织维度整体缺席，补入'组是第一结构'一条；[high] 新增的验证边界门在今天必然失败且与 AD-22 过渡期条款自相矛盾，改为绑定第 (1) 步之后生效的待建门；[medium] Codex 读 .agents/skills 属从 ides 三声明反推两目录的未核实推断（声明 3 个目录只有 2 个，映射非一一对应），按 AD-15 标注为 Unknown；[medium] pin 要求原本写死为 BMad 安装器专有产物，已一般化为每个 fork 组都必须有被跟踪的 pin。复跑 lint_spine 仍 0 findings。
- (event) architecture update finalized（2026-08-25，Skill 资产存放/组织/装配）：新增 AD-22 [ADOPTED]；能力→架构映射新增'本仓自我开发装配'行；验证边界新增 AD-22 一致性门（标注为第 (1) 步后生效的待建门）；Deferred 新增 Codex 侧入口与 .agents/skills 退役一条。AD-1~AD-21 的 Rule 正文一字未改，AD 编号未重排。lint_spine 0 findings；三 lens PASS with findings，1 critical + 2 high + 2 medium 全部已修。本轮只交付 spine，未产出额外文档。
- (decision) 2026-08-25 负责人裁决（#173 的两个问题，AD-22 退役第 (1) 步前提）：(a) skill 字节的每机来源 = 运行 BMad 安装器写 canonical 到 _bmad/。本轮已核实其可复现性（此前只是假设）：npm view bmad-method version 返回 6.11.0，与 _bmad/_config/manifest.yaml 的 installation.version 一致；两个 module 均 source: built-in（打包在该 npm 包内，无需另抓）；_bmad/_config/files-manifest.csv 记录 263 个文件、每条带 sha256，完整性可校验。(b) 原生启动的会话不再受支持，一律必须走 configs。
- (decision) 2026-08-25 由 (b) 推出的落点不变量：字节不得落在任何客户端的原生发现路径上，否则'必须走 configs'不可强制（会重演 entrypoints 那三条'加载可用的 X'把硬门降级为空操作的失败模式）。canonical 位置 _bmad/ 恰好不是原生发现路径，且 skill-manifest.csv 的 path 列本就指向 _bmad/core/<skill>/SKILL.md——这是 BMad 自身的设计形状，不是新发明。因此第 (1) 步的形状固定为：安装器按 pin 写 canonical 到 _bmad/（gitignored、每机复现）；ides 配置为不产生任何 IDE 投影；configs 从 canonical 路径物化进被它启动的会话。原生发现拿不到任何东西，规则由机制强制而非自觉。
- (constraint) 2026-08-25 核实发现：本仓当前的 BMad 安装是残缺的。files-manifest.csv 记录的 263 个文件全部位于 _bmad/ 下（176 个 bmm/、78 个 core/、5 个 scripts/、其余配置），而 _bmad/ 磁盘上只有 150 KB、九个配置文件——254 个 canonical 本体文件不存在，仓内只剩 .claude/skills 与 .agents/skills 两份 IDE 投影。这解释了此前 skill-manifest.csv 的 path 指向不存在路径之谜：不是安装器没写 canonical，是写完之后被删了、只留投影。退役第 (1) 步必须先把 canonical 恢复回来。
- (event) 2026-08-25 AD-22 修订完成（#173 两项裁定落地）：字节所有权条补入已核实的可复现性证据（npm bmad-method 6.11.0 与 pin 一致；files-manifest 263 条带 sha256，且经交叉比对 49/49 覆盖 skill 本体——这是'完整性可校验'成立的关键，专门验过）；新增'字节的落点不得是原生发现路径'一条 Rule；第 (1) 步写实两项前提并补入'本仓当前 BMad 安装残缺、254 个 canonical 缺失、必须先恢复'；可移植性段补入 canonical 落点为每机相同仓库相对路径这条强指示（不预先裁定）。Reviewer gate 按 stakes 缩放为两条配置 lens，均 PASS with findings，2 项 medium 全部 autofix：[current-tech] source built-in 的含义属推断非核实，已如实标注并挂到第 (1) 步待证实；[adversarial] '机制强制'覆盖面写得比实际大——用户级/机器级 skill 目录本仓管不到，已限定范围并规定第 (2) 步 parity 必须在无用户级 BMad 安装的环境中取证否则假阳性。lint_spine 0 findings。
- (decision) 2026-08-25 AD-22 回写：退役第 (1) 步的 sourceRef 可移植性开放项已关闭。裁定为库内相对 POSIX 路径唯一合法、绝对路径一律拒绝、根由单一共用 defaultSupplyRoot() 提供且不进修订；五条判定规则集中在唯一实现 validateSupplyRelativeRef，供给侧与解析侧共用且无任一侧单独覆盖入口，因此'禁止各自发明'从此由机制保证。已随 Story 3.4 落地并合并（8d56565）。第③④两条规则由实测逼出：known('')／known('.') 使 path.resolve 返回根本身、cp --recursive 把整个供给库当一个 Skill 拷入并报成功（fail-closed 被翻成 fail-open）；同盘 C:x/y 在 win32 被折进根内而接受且收敛判定转为依赖 cwd，C:/x/y 在 POSIX 上 isAbsolute 为假而被当相对名接受。兼容性依据：真实库 20 个 skill 条目 sourceRef 全为 Unknown、Known 零条。
- (event) 2026-08-25 同轮修复一处此前引入的文本缺陷：2026-08-25 早些时候的 AD-22 修订（PR #176）在替换第 (1) 步文本时未清掉锚点，导致'并且必须在这一步一并裁定 sourceRef 的跨机器可移植性语义——'一句连续重复两遍。本轮回写时发现并删除重复。lint_spine 修订前后均 0 findings（该 lint 不检测重复句）。
- (event) 2026-08-25 AD-22 回写完成。Reviewer gate 内联跑两条配置 lens（子 agent 授权只覆盖本轮 bmad-build 运行），落盘 reviews/review-ad22-writeback.md。current-tech PASS 无 findings（六条引用事实全部本 session 实测或直接观察）。adversarial PASS with findings，1 项 high 已当场修复：权威面从未说明根实际怎么取——AD-22 通篇只出现一次 defaultSupplyRoot，CONTROL_PLANE_SUPPLY_ROOT／部署场景／仓库根三词零出现，而同一条 AD 又把 canonical 落点定为仓库相对的 _bmad/；只读 spine 的 Story 3.5 实现者会推断用 $HOME 默认根，与 _bmad/ 拼起来解析不到任何东西且按 AD-10 静默降级。这与本 AD 当初成立的理由（成熟模型挂在工具 README 上、够不着权威面）是同一种病。已在落点那条 Rule 补入两种部署场景与各自的根来源。lint_spine 0 findings。
- (event) 2026-08-25 Story 3.6 落地后同步 AD-22：验证边界那条「自我开发装配一致性」从待建门改为现行门（实现 tools/assembly_intent/，在本机与 CI 两处真实执行）；Rule 里「当前已知不一致」那句改写为「已于 2026-08-25 消除」，并写明本条从此由机制维持——意图不由声明文件表达，而是从入口规则与 _bmad/_config/ 安装器清单推导，故改规则与改意图是同一个动作，构造上不可漂移。实测：门在真实仓 exit 0，推导出 3 个组 / 51 个 Skill；点名 Skill 不存在时 exit 2 并指名；入口措辞整体改写致提取落空时 exit 2 报「提取数为 0」且 supply 未被调用（这条反向断言是该门不沦为装饰的关键）。
