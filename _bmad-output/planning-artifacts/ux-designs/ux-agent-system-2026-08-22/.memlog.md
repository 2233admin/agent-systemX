---
topic: agent-system CLI 完整 UX 设计（此前从未设计过）
updated: 2026-08-22T16:20
---

- (decision) 输入来源确认：PRD(prd-agent-system-2026-08-21)、Brief(brief-agent-system-2026-08-21)、架构(architecture-agent-system-2026-08-22) 全部采用
- (decision) 风险等级/严肃程度：Hobby（个人项目，可访问性与错误处理从简）
- (note) Brief 提取：解决个人 Agent 资产（Instructions/Skills/MCP）碎片化问题；目标用户为高自治的个人实践者；双节奏（低频 Context Assembly / 高频 Profile Activation→Context Loading→Task Execution）；语气偏精确、证据克制、不夸大、安全诚实（显式 unknown，不静默成功）、少仪式感；人类仅在许可/高风险/不可逆/价值取舍点确认；brief 阶段未锁定 CLI 实现，TS 只是候选技术方向
- (note) PRD 提取：MVP 目标用户为高自治个人实践者；FR-1..14 定义两条工作流 WF-1(建立/修订稳定配置) WF-2(复用配置执行任务) WF-3(Bad Case 驱动的有界演进)；SM-6/FR-8 要求常规复用路径零单资产选择、零重复输入、至多一次确认；PRD 明确不定义 CLI/TUI 交互机制——留给架构/UX 阶段
- (note) 架构提取：CLI 是唯一组合根，生命周期 prepare→confirm-once→apply/launch→observe/reconcile；AD-3/AD-7 常规路径唯一一次确认，作用域为 operationId+planHash；AD-14 直接 argv spawn 不经 shell；AD-18 配置切换恒返回 requires-restart（无热切换）；AD-10 默认 fail-closed，结果状态为封闭集合 prepared|requires-restart|awaiting-confirmation|applying|observing|succeeded|degraded|failed|incomplete|cancelled；零退出码只证明 launched/exit 不证明 verified；MVP=Epic1 两个 Story：1.1 查看/比较配置内容，1.2 选择配置并通过 OMP 使用；命令名/TUI 文案/投影视图明确留给 UX 决定，但不得改变职责边界、状态所有权、一次确认上限
- (decision) 设计范围：只做 MVP（Epic 1 两个 Story：1.1 查看/比较配置内容，1.2 选择配置并通过 OMP 使用），不做未来方向前瞻设计
- (decision) 视觉气质：简洁克制，专业工具感（参照 git/gh），非个性化/非装饰性
- (decision) 工作定位：追认现有 CLI 行为为规范，同时寻找改进点（可能产生代码改动建议）
- (note) 现状代码勘察：bin 名为 configs；命令集 list/show/compare/use/status/switch 已完整实现；输出纯文本无色无图标缩进式；确认摘要含配置名/client/version/能力清单/已知差异/转发参数，一次性确认[y/N]或--yes；launchOmp 用 Bun.spawn stdio:['inherit','inherit','inherit'] 把终端完全交给 omp 进程，omp 退出后才打印最终 launch status——中间无进度提示；readYesNo 对空/关闭 stdin 视为拒绝（fail-fast，本分支刚修复过 hang bug）
- (decision) 改进点1：引入语义色彩（succeeded绿/failed·cancelled红/degraded黄等），需遵循 NO_COLOR 环境变量与非 TTY 自动降级为纯文本
- (decision) 改进点2：confirm 后、omp 接管终端(stdio inherit)前，打印一句过渡提示，明确告知控制权即将交给 omp
- (decision) 颜色角色映射与过渡提示文案方案确认，进入 Finalize：直接撰写 DESIGN.md / EXPERIENCE.md
- (change) 用户要求 DESIGN.md/EXPERIENCE.md 正文改用中文撰写（此前 draft 是英文），frontmatter 键名保持英文（spec 固定），值与正文改中文
- (decision) 改进点3：CLI 输出文本（字段标签/摘要/失败提示/交接提示行等面向用户阅读的说明性文本）全部改为中文，不做英文回退；命令名/参数名/flag（list/show/compare/use/status/switch、--yes、--client、[y/N]）保留英文标识符不变（argv 语法惯例）。这是相对现有代码的第三处实现改动建议。
- (event) DESIGN.md / EXPERIENCE.md 中文化改写完成，含文案对照表；确认 TUI 改造成本问题（建议维持纯文本，不换 TUI，理由：架构 AD-14/AD-15 明确排除 TUI）；接下来跑 bmad-review（structure+prose）作为标准收尾校验
- (change) 推翻此前'纯中文输出'决定，改为中英双语 i18n：默认中文，CONFIGS_LANG=en 显式切换（不跟随系统 LANG/LC_ALL，理由：与 NO_COLOR 同一套显式优先于隐式推断的心智模型）；EXPERIENCE.md 文案对照表已扩为 zh/en 双列
- (event) structure lens 校验完成：7 findings（1 MERGE, 3 QUESTION, 3 PRESERVE），采纳 MERGE（阶段枚举保留英文的理由去重）+ 3 个 QUESTION（退出码契约加粗前置、Key Flows 加一句定位句、Do's/Don'ts 交接行改为交叉引用）
- (event) structure findings 已应用：合并阶段枚举保留英文的重复说明、退出码契约加粗前置、Key Flows 加定位句；第4条(DESIGN.md Do's/Don'ts 交接行 vs EXPERIENCE.md 重复)评估后选择不动——Do's/Don'ts 表本身就该自包含可独立扫读，符合 reviewer 给出的备选保留理由
- (event) prose lens 校验完成：10 findings，均为语法/标点/术语一致性问题，无语气违规；准备逐条采纳
- (event) prose findings 全部采纳并应用（10条：语法补词、标点全角/半角、斜体统一为加粗、Foundation改动计数两处→三处、去重阶段枚举理由的第二次重复、阶段/状态术语混用两处修正为阶段、修订版本/版本混用两处修正、OMP版本→client版本、保护→维护）；两份 spine 状态设为 final，updated=2026-08-22
- (event) spines finalized
- (decision) 重大方向变更：引入 TUI，人机分工——人类交互默认走 TUI（configs 无参数启动），agent/自动化装配走现有纯文本 argv CLI（显式子命令，脚本化不受影响）。已用真实 .cap 数据跑通现有 CLI 演示，暴露真实痛点：list 展示的 revision id 是完整 sha256，show/use/compare 必须原样复制粘贴该 hash，配置名不能直接当 id 用；无参数运行只报 usage-error 无引导。这是这次 TUI 决策的直接证据来源。
- (decision) TUI 技术选型：ink（React 模式）；先出 Excalidraw 线框图确认屏幕流布局，再回写 spine
- (decision) TUI 屏幕流进一步简化：去掉页面3(compare，延后到纯文本CLI) 和页面5(独立结果屏)。omp 退出后不回到 TUI 列表循环——直接打印一次纯文本最终状态（复用现有 renderLaunchStatus 输出）后整个 configs 进程退出，回到真实 shell。拒绝确认(n/esc)则保持在 TUI 内回到列表，不退出。最终 TUI 范围收敛为3屏：浏览列表→详情(可选)→确认+启动，单向流程，不循环。
- (decision) 重大简化（负责人明确拍板，非普通UX决定）：TUI 去掉独立确认屏，选中配置按 u 直接启动。技术上复用 --yes 已有的 confirmLaunchPlan 自动确认路径（不改领域模型/LaunchPlan状态机，只是TUI从不要求人交互式回答那次确认）。已知差异的可见性用另一种方式保留：浏览列表每行直接标记有已知差异的配置（颜色/标签），详情屏直接展示完整已知差异内容（不再等到确认摘要才展示）。理由：CLI 走确认摘要屏是因为纯文本一次只能展示一层信息，TUI 可以在列表和详情里分层直接展示，不需要专门用一次确认动作来兜底信息暴露。TUI 屏幕数进一步收敛为2屏：浏览列表（含差异标记）→ 详情（含完整差异）；u 键从任一屏直接触发启动/交接序列，无独立确认屏。
- (event) TUI spine 改写完成：DESIGN.md 新增双界面视觉规约（TUI 组件：tui-list-row/tui-detail-panel/tui-footer-hints，reverse video 选中，无边框只用分隔线，Elevation/Shapes 章节补充 TUI 内容）；EXPERIENCE.md 新增 Foundation 双界面分工、IA 第三层(TUI两屏)、Component Patterns(TUI列表行/详情面板)、Interaction Primitives(TUI键位、TUI自动确认的完整授权说明、终端交接的进程存活澄清、一次性退出不循环)、State Patterns两处状态注记、Accessibility Floor三条TUI条目、Key Flows重写Wren旅程为TUI优先+新增agent自动化路径对照段、i18n文案表补3行TUI文案。两份文件状态改回 draft，待重新校验。
- (change) 用户纠正：AI agent 不消费装配后的 omp、不调用 use/switch，只协助装配。修正了 DESIGN.md/EXPERIENCE.md 里三处错误框架（frontmatter description、Foundation 的'自动化/agent 路径'措辞、Key Flows 里编造的'agent-assembler 调用 configs use --yes'对照段）——统一改为'非交互/脚本化路径'，并明确启动 OMP 会话永远是人类为自己触发的动作，agent 只协助装配（不在本 CLI 范围内）。
- (event) structure lens 重新校验完成：6 条可执行建议（1 补全DESIGN.md缺失的TUI组件条目、2 CUT/CONDENSE去重agent措辞、1 MERGE合并Foundation两个bullet的重复断言、2 MOVE重排Interaction Primitives顺序+Foundation范围外列表格式化）+ 2 条 PRESERVE；准备逐条采纳
- (event) 全部6条structure findings已应用：补全DESIGN.md 3个TUI组件条目、删掉Brand&Style delta段落里的agent旁白、合并Foundation两个bullet的重复断言、Interaction Primitives重排(CLI的确认+模糊输入两条紧邻，TUI自动确认后置)、Foundation范围外列表改bullet格式、Key Flows结尾'容易搞混的边界'段精简为交叉引用Foundation
- (event) prose lens 重新校验完成：11 条 findings，均为语法/一致性问题（重点：确认/跳过 措辞在两处与'TUI 不是跳过而是自动确认'的核心论点自相矛盾；IA 章节'两层入口'漏改成'三层'；重排后'见下一条'指针失效；交接提示行触发条件描述只写了 CLI 未覆盖 TUI），无语气违规；逐条采纳
- (event) 全部11条prose findings已应用（含最重要的：修正两处'确认被跳过'措辞使其与'TUI是自动确认非跳过'的核心论点一致；IA'两层'改'三层'；重排后失效的'见下一条'指针修正；交接提示行触发条件补上TUI分支；i18n表{id}占位符消歧为{revisionId}/{planId}）。两份文件状态恢复 final，updated=2026-08-22。
- (event) spines finalized (TUI revision)
