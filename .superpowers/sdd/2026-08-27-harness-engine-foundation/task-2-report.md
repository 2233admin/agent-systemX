# Task 2 Implementation Report

## Changed files

- `packages/harness-engine/src/domain/workflow.ts`
  - Added `PlanStatus`, `PlanRow`, `WorkflowSnapshot`, `ExecutionLease`, `IntegrationMergeLease`, and `CompletionEvidence` contracts.
  - Added pure `transitionPlanStatus` and `isPlanStatus` functions.
  - Completion to `Done` requires explicit evidence and rejects remaining leases or missing required review/QA evidence.
- `packages/harness-engine/src/ports/artifacts.ts`
  - Added the `ArtifactStore` port with workflow read/write operations.
- `packages/harness-engine/src/adapters/json/json-artifact-store.ts`
  - Added version-1 JSON persistence under `<root>/workflows/<workflowId>.json`.
  - Reads missing workflows as `null`, rejects malformed and future schema versions, checks revisions, creates parent directories, and replaces artifacts through a same-directory temporary file plus rename.
  - Serializes explicit DTO columns and removes prompt, transcript, credential, tool-payload, and dynamic-task fields from persisted data.
- `packages/harness-engine/tests/domain/workflow.test.ts`
  - Covers all five status values as sources and destinations, plus Done preconditions.
- `packages/harness-engine/tests/adapters/json-artifact-store.test.ts`
  - Covers missing reads, versioned round trips, privacy filtering, revision conflicts, future schema rejection, and atomic replacement.
- `packages/harness-engine/src/index.ts`
  - Exported the new domain contracts, port, adapter, and transition helpers.

## Tests run

- `bun test packages/harness-engine/tests/domain/workflow.test.ts packages/harness-engine/tests/adapters/json-artifact-store.test.ts`
  - Observed: 11 pass, 0 fail, 74 assertions.
- `bunx tsc --noEmit -p packages/harness-engine/tsconfig.json`
  - Observed: pass with no output.
- Combined acceptance command (`bun test ... && bunx tsc ...`)
  - Observed: pass with 11 tests passing and typecheck passing.

## Public API summary

- `PlanStatus = 'Todo' | 'InProgress' | 'InReview' | 'Blocked' | 'Done'`.
- `transitionPlanStatus(plan, nextStatus, completionEvidence?)` returns a new immutable plan row and validates completion preconditions.
- `WorkflowSnapshot` carries schema version, revision, workflow identity, plan rows, optional integration merge lease, and optional read-time `updatedAt`.
- `ArtifactStore` exposes `readWorkflow(workflowId)` and optimistic-concurrency `writeWorkflow(expectedRevision, next)`.
- `JsonArtifactStore` is the first concrete store and uses only filesystem JSON operations.

## Concerns

- The JSON store intentionally supports only schema version 1 and rejects future versions rather than migrating them.
- `updatedAt` is generated by the store on every write and is optional only when constructing an in-memory snapshot.
- Lease objects remain extensible for coordination metadata, while runtime prompt/transcript/credential/tool-payload/dynamic-task fields are filtered from serialized JSON.
